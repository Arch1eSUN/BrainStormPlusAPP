# Progress: Sprint 2.6 (Projects Risk Action Sync Write Path Foundation)

## Completed
- 1.0 Navigation & Tasks Create Flow certified complete by `docs/parity/14-winston-audit-1.0-0.4.md`.
- 1.1 Task Status Enum + Picker Parity certified complete by `docs/parity/18-winston-audit-1.1-0.2.md`.
- 1.2 Dashboard Destination Parity certified complete by `docs/parity/20-winston-audit-1.2.md`.
- 1.3 Projects List Foundation certified complete by `docs/parity/22-winston-audit-1.3.md`.
- 1.4 Projects Filter + Detail Foundation certified complete by `docs/parity/24-winston-audit-1.4.md`.
- 1.5 Projects Server-Side Filter + Membership Scope Foundation certified complete by `docs/parity/26-winston-audit-1.5.md`.
- 1.6 Projects Detail Membership Gate + Ordering Alignment Foundation certified complete by `docs/parity/28-winston-audit-1.6.md`.
- 1.7 Projects Owner Profile Join + Detail Enrichment Foundation certified complete by `docs/parity/30-winston-audit-1.7.md`.
- 1.8 Projects Nested Profile Join + Avatar Rendering Foundation certified complete by `docs/parity/32-winston-audit-1.8.md`.
- 1.9 Projects Edit + Member Management Foundation certified complete by `docs/parity/34-winston-audit-1.9.md`.
- 2.0 Projects Delete Foundation certified complete by `docs/parity/36-winston-audit-2.0.md`.
- 2.1 Projects Task Count List Parity certified complete by `docs/parity/38-winston-audit-2.1.md`.
- 2.2 Projects AI Summary Foundation certified complete by `docs/parity/40-winston-audit-2.2.md`.
- 2.3 Projects Risk Analysis Foundation certified complete by `docs/parity/42-winston-audit-2.3.md`.
- 2.4 Projects Linked Risk Actions Foundation certified complete by `docs/parity/44-winston-audit-2.4.md`.
- 2.5 Projects Resolution Feedback Foundation built:
  - **Goal A — Source-of-truth read of Web first**:
    - Re-read `BrainStorm+-Web/src/lib/actions/summary-actions.ts` `getProjectRiskResolutionSummary(projectId)` at lines 630-714: `'use server'` action that does a two-step read — (1) `adminDb.from('project_risk_summaries').select('id').eq('project_id', projectId).maybeSingle()` to locate the risk-summary anchor id; if no anchor row exists, returns `{ data: null, error: null }` (NOT an error). (2) `adminDb.from('risk_actions').select('title, status, severity, resolution_category, effectiveness, follow_up_required, reopen_count, resolved_at').eq('ai_source_id', summary.id).order('resolved_at', { ascending: false, nullsFirst: false }).limit(50)`. Then client-side aggregation computes `total/resolved/dismissed/active/followUpRequired/reopenedCount/dominantCategory/recentResolutions`.
    - Re-read `BrainStorm+-Web/src/app/dashboard/projects/page.tsx` lines 725-838:
      - Lines 754-758: predictive pulsing rose "易重开" badge when `reopenedCount > 0 && active > 0`.
      - Lines 780-796: governance-signal badges — "干预已生效" (green tone) when any recent resolution is `effectiveness === 'effective' && category === 'root_cause_fixed'`; "待治理干预" (red tone) when `reopenedCount > 0 && active > 0` (same trigger as "易重开").
      - Count badges: resolved / dismissed / active / followUpRequired / reopenedCount — labeled inline in the dashboard render.
      - `dominantCategory` labeled via a small `category → 中文 label` map.
      - `recentResolutions` top-3 rendered as compact rows (title + status + effectiveness + `resolved_at`).
    - Re-read `BrainStorm+-Web/src/app/api/` — NO `/api/risk-resolution*` HTTP route. `getProjectRiskResolutionSummary` is server-action-only; unlike 2.2 / 2.3, its guts are **pure Supabase reads with no `askAI()` call and no server-decrypted `api_keys` access**, so iOS CAN faithfully replicate the flow via direct PostgREST.
    - Re-read migration `037_round8_risk_knowledge_ai.sql` + extensions 015-017: `risk_actions.resolution_category ∈ {root_cause_fixed, workaround_applied, escalated, deferred, false_positive}`, `effectiveness ∈ {effective, partial, ineffective, pending}`, `follow_up_required BOOL DEFAULT false`, `reopen_count INT DEFAULT 0`, `resolved_at TIMESTAMPTZ`. RLS: org-scoped SELECT permits iOS reads; INSERT/UPDATE gated to `['super_admin', 'admin', 'hr_admin', 'manager']` (write path explicitly deferred).
    - Conclusion recorded as **NO source-of-truth discrepancy this round** (same parity posture as 2.4): pure-read flow faithfully replicable on iOS. Resolution write-back (close / reopen / effectiveness / governance note) + "转为风险动作" sync remain explicitly deferred by scope (devprompt §3.G), not by source-of-truth divergence.

  - **Goal B — Foundation data struct**:
    - `Brainstorm+/Features/Projects/ProjectDetailModels.swift` (modified): new public `ProjectResolutionFeedback: Equatable` with `total: Int`, `resolved: Int`, `dismissed: Int`, `active: Int`, `followUpRequired: Int`, `dominantCategory: String?`, `reopenedCount: Int`, `recentResolutions: [RecentResolution]`. Nested `RecentResolution: Equatable, Hashable` with `title`, `status`, `category: String?`, `effectiveness: String?`, `resolvedAtRaw: String?`. Nested `GovernanceSignal: Equatable { .none, .interventionEffective, .needsIntervention }` enum.
    - Two computed properties on the struct: `governanceSignal` (mirrors Web's derivation; see 2.5.1 note below for the corrected priority) and `isProneToReopen` (true when `reopenedCount > 0 && active > 0`, mirrors Web's predictive "易重开" trigger, rendered as an independent rail from governance status).
    - Status / effectiveness / category decoded as `String` (not typed enums) so unexpected server vocabularies don't crash foundation decode — UI styling defends via switches with neutral fallback.
    - `resolvedAtRaw` kept as `String?` so both `timestamptz` formats (with/without fractional seconds) decode; parsed on render in the view helper, not on decode.
    - Extensive doc comment cites Web source-of-truth file + lines (630-714 for the server action, 725-838 for the page renderer), records `total` capping at 50 matches Web exactly (not a divergence), records governance priority resolution.

  - **Goal C — ViewModel state machine**:
    - `Brainstorm+/Features/Projects/ProjectDetailViewModel.swift` (modified): new `public enum ResolutionFeedbackPhase: Equatable { case idle, loading, noRiskAnalysisSource, empty, loaded }` and three new `@Published` properties after the 2.4 linked-actions block:
      - `resolutionFeedback: ProjectResolutionFeedback? = nil`
      - `resolutionFeedbackPhase: ResolutionFeedbackPhase = .idle`
      - `resolutionFeedbackErrorMessage: String? = nil` — isolated error surface, side-band to the phase enum so a transient failure never wipes a previously-aggregated snapshot.
    - `applyDeniedState()` extended to clear all three resolution-feedback fields alongside the existing access-denial cleanup (+ 2.4 linked-actions cleanup).
    - New `public func refreshResolutionFeedback() async` entry point:
      - Captures `priorPhase` before flipping `resolutionFeedbackPhase = .loading` and clearing `resolutionFeedbackErrorMessage = nil`.
      - **Step 1 (anchor lookup)**: reads `project_risk_summaries.select("id").eq("project_id", value: projectId).limit(1).execute().value` as `[LinkedRiskAnchorRow]` (reused from 2.4 — both flows share the anchor shape). Zero rows → sets `resolutionFeedback = nil`, phase `.noRiskAnalysisSource`, returns cleanly.
      - **Step 2 (filtered select)**: reads `risk_actions.select("title, status, severity, resolution_category, effectiveness, follow_up_required, reopen_count, resolved_at").eq("ai_source_id", value: anchor.id).order("resolved_at", ascending: false, nullsFirst: false).limit(50).execute().value` as `[ResolutionFeedbackRow]`. Zero rows → phase `.empty` + `resolutionFeedback = nil`; non-empty → `resolutionFeedback = Self.aggregateResolutionFeedback(rows:)`, phase `.loaded`.
      - **Failure path**: preserves the prior `resolutionFeedback` snapshot (transient flakiness doesn't wipe valid context), writes `resolutionFeedbackErrorMessage = error.localizedDescription`, reverts `resolutionFeedbackPhase` back to `priorPhase` (or `.idle` if `priorPhase == .loading`). Does NOT touch `errorMessage`, `enrichmentErrors`, `deleteErrorMessage`, `summaryErrorMessage`, `riskAnalysisErrorMessage`, `linkedRiskActionsErrorMessage`, or `access`.
    - Private Decodable DTO `ResolutionFeedbackRow` with the 8 fields Web's select projection reads. `CodingKeys` maps snake_case → camelCase. Kept private to the VM.
    - New private static helper `aggregateResolutionFeedback(rows:) -> ProjectResolutionFeedback`: runs Web's exact counts via Swift filters, builds `dominantCategory` with first-encountered tie-break (rows arrive `resolved_at DESC NULLS LAST`, so the tie-break is stable across runs), slices top-3 resolved/dismissed into `recentResolutions`.
    - Verified Supabase Swift SDK `order(_:ascending:nullsFirst:referencedTable:)` signature directly at `PostgrestTransformBuilder.swift:44` — `nullsFirst: false` is a real parameter; usage is correct.

  - **Goal D — Detail view surface**:
    - `Brainstorm+/Features/Projects/ProjectDetailView.swift` (modified): new `resolutionFeedbackSection` inserted in `detailScroll` AFTER `linkedRiskActionsSection`, BEFORE the `errorMessage` banner + `foundationScopeNote`. Section anchored directly below linked-actions because its data model aggregates over the same `risk_actions` filter the 2.4 section lists. Section header: "Resolution Feedback" + conditional "{total} tracked" count badge shown only in `.loaded` phase. Subtitle: "Read-only · resolution write-back and governance interventions are only available on the web." — honest labeling of the foundation scope.
    - `@ViewBuilder resolutionFeedbackBody` switching on phase:
      - `.idle` / `.loading` → `EmptyView()` (the button carries the UI state for these phases).
      - `.noRiskAnalysisSource` → hint row: "No risk analysis exists for this project yet. Run one from the web dashboard first, then come back to view its resolution feedback here." (points the user to run 2.3's flow first.)
      - `.empty` → hint row: "No risk actions tracked yet for this analysis, so there's no resolution feedback to aggregate."
      - `.loaded` → stacks five composable rows: `resolutionCountsRow` (horizontal pill strip of five badges) + `governanceBanner` (only when `.governanceSignal != .none || isProneToReopen`) + `dominantCategoryRow` (only when category non-nil/non-empty) + `recentResolutionsList` (top-3 rows).
    - `resolutionCountsRow` renders horizontal-scroll pill strip: `Resolved` (primary/primaryLight), `Dismissed` (textSecondary/gray×15%), `Active` (warning/warning×18%), `Follow-up` (warning/warning×18%), `Reopened` (white/red×85%).
    - `governanceBanner` renders icon + title + optional "Prone to reopen · {N} reopened action(s) still have unresolved work." sub-line. Tones:
      - `.interventionEffective` → `checkmark.shield.fill` + "Intervention effective" + `Color.Brand.primary` on `Color.Brand.primaryLight.opacity(0.55)`.
      - `.needsIntervention` → `exclamationmark.shield.fill` + "Needs governance intervention" + `Color.Brand.warning` on `Color.Brand.warning.opacity(0.18)`.
    - `dominantCategoryRow` renders tag icon + "Dominant category" + humanized label (e.g. `root_cause_fixed` → `Root Cause Fixed`) via `Self.humanize(_:)` — English foundation copy, i18n carry-forward debt.
    - `recentResolutionRow` renders 8pt status dot (via `resolutionStatusColor(_:)`) + title (2 lines) + effectiveness capsule (via `effectivenessCapsule(effectiveness:)`) + parsed resolved_at date. Date parsing via `resolvedAtDisplayDate(_:)` tries both `.withInternetDateTime` and `.withFractionalSeconds` — returns `nil` on any parse failure so render drops the date element rather than crashing.
    - `effectivenessStyle(for:)` palette (defensive switch; unknown → humanized label on neutral):
      - `effective` → primary on primaryLight.
      - `partial` → warning on warning×18%.
      - `ineffective` → white on `Color.red.opacity(0.85)`.
      - `pending` → textSecondary on gray×15%.
    - `resolutionStatusColor(_:)` narrow palette (recent list only surfaces `resolved|dismissed`): `resolved → .green`, `dismissed/default → textSecondary`.
    - `governanceStyle(for:)` palette maps each signal to `(title, foreground, background)` tokens; `.none` branch is dead-code defensive (callers already guard).
    - Button state machine (`resolutionFeedbackActionButton`):
      - `.idle` → `Label("Check for resolution feedback", systemImage: "checkmark.seal")`.
      - `.loading` → `Label("Checking…", systemImage: "checkmark.seal")` + trailing `ProgressView()` + disabled.
      - `.loaded` / `.empty` / `.noRiskAnalysisSource` → `Label("Refresh", systemImage: "checkmark.seal")`.
      - Error (any phase with `resolutionFeedbackErrorMessage != nil`) → `Label("Try again", systemImage: "checkmark.seal")`.
      - Button disabled while `isLoading || isDeleting`.
    - Button tap fires `Task { await viewModel.refreshResolutionFeedback() }`.
    - Soft scoped error row reuses the existing `summaryErrorRow(_:)` builder — consistent with 2.2 / 2.3 / 2.4 error affordances.
    - `foundationScopeNote` copy updated: "Converting risks into actions and generating new risk analyses from iOS are available on the web and will arrive in later iOS rounds." (supersedes 2.4's copy — "resolution feedback" is no longer in the deferred list since 2.5 closed that gap.)
    - Visual language reuses existing `enrichmentCard` token (`Color.Brand.paper`, 20 pt corner radius, 0.04 shadow) — no new design token, no new corner radius.

  - **Goal E — State truth / isolation**:
    - No regression to 1.5 membership scoping, 1.6 detail gate + `created_at DESC` ordering, 1.7 owner hydrate, 1.8 nested profile hydrate + avatar rendering, 1.9 edit + member management, 2.0 delete flow, 2.1 task-count-on-list, 2.2 AI summary foundation, 2.3 risk-analysis foundation, or 2.4 linked-risk-actions foundation.
    - `resolutionFeedbackErrorMessage` is a NEW error surface, distinct from `errorMessage`, `enrichmentErrors`, `deleteErrorMessage`, `summaryErrorMessage`, `riskAnalysisErrorMessage`, `linkedRiskActionsErrorMessage`, and the `access == .denied` path. A resolution-feedback read failure is decorative — cards + sublists + AI summary section + risk analysis section + linked-actions section + action bar all stay on screen with only the resolution-feedback section flipping to the error state.
    - `applyDeniedState()` extension ensures stale resolution-feedback snapshots from a prior `.allowed` fetch cannot leak into a subsequent `.denied` state.
    - Success path clears `resolutionFeedbackErrorMessage` back to `nil`.
    - Failure preserves the prior `resolutionFeedback` snapshot (transient flakiness doesn't wipe valid context) and reverts phase to `priorPhase` so the UI doesn't get stuck on "Loading…".
    - No new global state, no new navigation destination, no new sheet modal.

  - **Goal F — Web parity mapping**:
    - Read call shape: two sequential PostgREST reads against the same tables + columns + order + limit Web's server action hits — `project_risk_summaries.select("id")` as anchor, then `risk_actions.select(...8 columns...).eq("ai_source_id", anchor.id).order("resolved_at", ascending: false, nullsFirst: false).limit(50)`. Column projection matches Web exactly.
    - Client-side aggregation mirrors Web's exact counts (resolved / dismissed / active {open, acknowledged, in_progress} / followUpRequired true / reopenedCount > 0), `dominantCategory` first-encountered tie-break (stable under rows-arrive-`resolved_at DESC`), `recentResolutions` top-3 filter to `{resolved, dismissed}` then `prefix(3)`.
    - `total` caps at 50: matches Web exactly (both apply the aggregation over a limit-50 result set; NOT a divergence).
    - Swift SDK divergence: `.limit(1) + rows.first` substitutes for Web's `.maybeSingle()`. Established 1.6 / 2.3 / 2.4 pattern.
    - Governance signal derivation mirrors Web's logic at `page.tsx` 780-796. The 2.5.1 fix corrects the priority to **effective-first**: `hasEffective` short-circuits before `needsIntervention` is checked (matches Web). The predictive "易重开" indicator stays on an independent rail so it can render alongside `.interventionEffective` without being drowned by the banner's primary tone.
    - Predictive "易重开" signal: iOS folds it into the governance banner's sub-line (`isProneToReopen`) rather than rendering a separate pulsing pill — same trigger, different affordance.
    - Count badges mirror Web: 5 pills (resolved / dismissed / active / follow-up / reopened). Iconless, horizontal-scroll to avoid compression on narrow widths.
    - Entry point divergence (intentional): Web auto-fetches `resolutionFeedback` inside `loadProjectDetail`; iOS gates behind an explicit "Check for resolution feedback" button tap. Rationale: avoids cascading two additional round-trips into the initial detail load (which is already doing 4 `async let` reads + enrichment + owner hydrate + nested profile hydrate + task-count aggregate + AI summary + risk analysis + linked-actions), keeps the foundation UX honest about what triggers network, and mirrors 2.2 / 2.3 / 2.4 explicit-tap posture. Recorded as foundation divergence, not a bug.
    - Resolution write-back (close / reopen / add effectiveness / add governance note) + "转为风险动作" sync button: not surfaced on iOS. Devprompt §3.G explicitly excludes all write paths. Stays on Web until a later round promotes the relevant writes to a reachable endpoint or mirrors the RBAC client-side.
    - Failure surface: Web is silent on error (empty render); iOS uses `resolutionFeedbackErrorMessage` scoped to the resolution-feedback section. iOS stronger — visible failure with phase revert.

  - **Goal G — Scope control**: no resolution write-back, no governance note write, no "转为风险动作" sync button, no `syncRiskFromDetection` write path, no risk item grouping, no streaming, no persistence layer on iOS, no offline cache, no schema changes, no new columns/indexes/views, no RLS changes, no new HTTP endpoint, no new Edge Function, no new `api_keys` touchpoint, no embedding of provider credentials on-device, no force-regenerate flow, no generate-risk-from-iOS, no auto-fetch on detail load, no create/edit/delete/member/task-count redesign. `.projects` remains `.partial`.

  - **Goal H — Ledger truth**:
    - `progress.md` retitled to Sprint 2.5; captures deliverables, skills used, and remaining gaps.
    - `findings.md` rewritten for 2.5 scope; delta tables flip resolution-feedback entry / loading state / two-step read / noRiskAnalysisSource state / empty state / aggregation / counts / governance signal / prone-to-reopen / dominant category / top-3 recent resolutions / effectiveness capsule / resolved_at parser / failure isolation / prior-snapshot preservation rows to **Delivered (2.5)**; explicitly lists **resolution write-back / governance intervention write / "转为风险动作" sync / generate-from-iOS / LLM narrative / risk_items JSONB** as **Deferred (out-of-scope)**.
    - `task_plan.md` marks `2.5 Projects Resolution Feedback Foundation completed` and next-step Winston 2.5 audit.
    - New `docs/parity/45-winston-ready-2.5-notes.md` Winston-ready notes.

  - `AppModule.projects` remains `.partial` — resolution write-back, governance intervention write, "转为风险动作" sync, the LLM narrative on summary, and "generate risk from iOS" are all still missing. 2.5 closes the resolution-feedback-foundation gap on detail but does not promote to `.full`.
  - Independent build passed (`** BUILD SUCCEEDED **`) on `iPhone 17 Pro Max` destination with `CODE_SIGNING_ALLOWED=NO`.
  - Verification scan count **106 across 3 files** matching `'getProjectRiskResolutionSummary|resolution_category|effectiveness|follow_up_required|reopen_count|resolved_at|resolutionFeedback|governance|ProjectResolutionFeedback|GovernanceSignal|isProneToReopen'` inside `Brainstorm+/Features/Projects` (`ProjectDetailModels.swift: 16`, `ProjectDetailViewModel.swift: 48`, `ProjectDetailView.swift: 42`).

- **Tools/Skills**: `planning-with-files` (single-pass ledger update across findings / progress / task_plan / notes after the code changes settled). `verification-before-completion` (devprompt §4 prescribed scan + final `xcodebuild` before claiming done; BUILD SUCCEEDED captured; scan count 106 across 3 files). `systematic-debugging` (Explore-agent deep read of `BrainStorm+-Web/src/lib/actions/summary-actions.ts` `getProjectRiskResolutionSummary`, `BrainStorm+-Web/src/app/dashboard/projects/page.tsx` resolution feedback render block, a survey of `BrainStorm+-Web/src/app/api/` to confirm there is no `/api/risk-resolution*` HTTP route, migration confirmation of the `risk_actions` extended columns + RLS — critical input for the pure-read parity framing). `receiving-code-review` (Winston 2.4 audit's "Recommended next" named resolution feedback as the natural 2.5 target; 2.5 scope sheet aligns). `clarify` (no clarification round needed — devprompt spelled out entry points, state isolation rules, phase-transition rules, governance-signal priority, and predictive-"易重开" trigger unambiguously). `supabase-postgres-best-practices` substituted by in-repo SDK pattern review — `.from("...").select(...).eq(...).order(..., nullsFirst: false).limit(...).execute().value` + two-step anchor-then-filter is the established 1.6 / 2.3 / 2.4 pattern; verified `nullsFirst:` parameter signature directly against `PostgrestTransformBuilder.swift:44`. Skill files for `ios-development-best-practices` and `avoid-feature-creep` not present under `~/.claude/skills/`; substituted by in-repo SwiftUI patterns + devprompt §3.G scope discipline — `@MainActor` `ObservableObject`, five-phase enum + side-band error, `priorPhase` capture + revert on failure, `@ViewBuilder` switch on phase, state-driven button copy + icon, scoped error row inside the enrichment-card layer, defensive status/effectiveness/category switch with neutral fallback. `native-data-fetching` — two-step anchor-then-filter read + client-side aggregation with preservation of prior snapshot on transient failure; single-shot "Check for resolution feedback" / "Refresh" pattern instead of auto-fetch.
- Winston 2.4 audit completed and passed: `docs/parity/44-winston-audit-2.4.md`.
- 2.5 prompt created: `devprompt/2.5-projects-resolution-feedback-foundation.md`.
- Winston 2.5 audit completed and failed: `docs/parity/46-winston-audit-2.5.md`.
- 2.5.1 minimum fix prompt created: `devprompt/2.5.1-resolution-feedback-governance-priority-fix.md`.
- Winston 2.5.1 audit completed and passed: `docs/parity/48-winston-audit-2.5.1.md`.
- 2.6 prompt created: `devprompt/2.6-projects-risk-action-sync-write-path-foundation.md`.

- **2.5.1 Resolution Feedback Governance Priority Fix delivered**:
  - **Goal A — Fix `governanceSignal` priority**: `ProjectDetailModels.swift` now evaluates `effective` first (`effectiveness == "effective" && category == "root_cause_fixed"` over `recentResolutions`), returns `.interventionEffective` when true; only if false does it evaluate `reopenedCount > 0 && active > 0` and return `.needsIntervention`; otherwise `.none`. Mirrors Web's `hasEffective`-first short-circuit at `BrainStorm+-Web/src/app/dashboard/projects/page.tsx` lines 780-796 exactly.
  - **Goal B — Keep `isProneToReopen` as an independent rail**: trigger unchanged (`reopenedCount > 0 && active > 0`). Continues to render as a subline inside the governance banner whenever it fires, including alongside `.interventionEffective`.
  - **Goal C — Visual semantic separation**: `ProjectDetailView.swift` switched the subline color from `tint.opacity(0.9)` (which inherited the governance-banner tint) to `Color.Brand.warning` (invariant across governance signals), so a green "Intervention effective" banner cannot drag the warning subline into its own primary hue. Header + banner background still follow governance tone; only the subline text color is stabilized.
  - **Goal D — Comments and docstrings**: `ProjectResolutionFeedback.governanceSignal` docstring rewritten to describe "effective-first" priority and the independent `isProneToReopen` rail. Removed "danger trumps success", "needsIntervention wins priority when both fire", and the erroneous "single-banner affordance" framing.
  - **Goal E — Ledger**: `findings.md`, `progress.md`, `task_plan.md` all updated. Prior debt lines about the mis-implemented priority are now struck-through with a "Resolved in 2.5.1" note so the audit trail stays visible without misleading future readers. `docs/parity/47-winston-ready-2.5.1-notes.md` created as the Winston-ready snapshot.
  - **Goal F — Scope discipline**: nothing else was touched. No changes to the two-step read, aggregation, phase enum, state isolation, button copy, error rail, AI summary, risk analysis, linked actions, delete, edit, member management, task count, list, or routing. `.projects` stays `.partial`. No schema / RLS / Web / i18n changes.
  - **Verification**: `xcodebuild build -project Brainstorm+.xcodeproj -scheme Brainstorm+ -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" CODE_SIGNING_ALLOWED=NO` re-run post-fix; result captured in the 47-winston-ready notes. Scan `'governanceSignal|interventionEffective|needsIntervention|isProneToReopen|danger trumps success|effective-first|hasEffective'` returns zero live hits for the forbidden phrases (only historical struck-through debt lines retain them for audit trail; the devprompt file itself is expected to still contain the forbidden phrase inside its "不得再写" directive).

- **2.6 Projects Risk Action Sync Write Path Foundation delivered**:
  - **Goal A — Source-of-truth read of Web first**:
    - Re-read `BrainStorm+-Web/src/lib/actions/risk-actions.ts` lines 310-394: `createRiskAction(params)` does (1) `serverGuard({ requiredRole: 'manager' })` client role assertion, (2) `title.trim()` empty reject, (3) `supabase.from('profiles').select('org_id').eq('id', user.id).single()` for org scope, (4) `supabase.from('risk_actions').insert({ org_id, risk_type: type ?? 'manual', source_type, source_id, ai_source_id, title: title.trim(), detail, severity: severity ?? 'medium', suggested_action, status: 'open', created_by: user.id }).select().single()`, (5) best-effort `logRiskEvent(action.id, 'created', { toStatus: 'open' })` — failures swallowed. `syncRiskFromDetection` at lines 374-394 is a thin wrapper that forwards args verbatim.
    - Re-read `BrainStorm+-Web/src/app/dashboard/projects/page.tsx` lines 205-224 + 687-697: `handleSyncToRiskAction` passes `{ type: 'manual', title: '[${detail.name}] 风险项', detail: riskSummary.slice(0,200), severity: riskLevel === 'critical' || riskLevel === 'high' ? 'high' : riskLevel === 'medium' ? 'medium' : 'low', suggestedAction: '从项目风险分析生成', sourceType: 'project', sourceId: detail?.id ?? '', aiSourceId: riskSummaryId ?? undefined }`. On success: refreshes linked actions. Auto-clears sync message 3s later. NO client-side RBAC pre-check — Web relies on RLS post-error.
    - Re-read `BrainStorm+-Web/src/lib/security/rbac.ts` + `auth-helpers.ts`: `serverGuard({ requiredRole: 'manager' })` resolves to role set `['super_admin', 'admin', 'hr_admin', 'manager']`.
    - Re-read Supabase migrations for `risk_actions` + `risk_action_events`: INSERT policy gates on role set `['super_admin', 'admin', 'hr_admin', 'manager']`; all FK constraints satisfied by (`org_id`, `source_id`, `ai_source_id`, `created_by`) derivable client-side without LLM / server secrets. `risk_items` JSONB column exists but is optional — Web's sync flow does not populate it either. `risk_action_events` best-effort audit row: `action_id, event_type, to_status, actor_id`; `from_status` and `note` are optional on the 'created' event.
    - **Path decision**: **Path A (direct write feasible)**. All columns populated client-side; no `askAI()` / `decryptApiKey()` / `api_keys` dependency; no server-only enrichment; RLS gates the server side; client RBAC gate mirrors the policy role set exactly. Zero source-of-truth discrepancy on the primary insert.

  - **Goal B — Client-side RBAC gate that does NOT drift from server RLS**:
    - Devprompt §3.A bans "relying on RLS post-error as the sole UX". Audited `Brainstorm+/Shared/Security/RBACManager.swift`: `migrateLegacyRole(_:)` maps `super_admin → .superadmin`, `admin | manager | team_lead → .admin`, `chairperson → .chairperson`, and critically leaves `hr_admin` UNMAPPED (falls through to `.employee`). Using `PrimaryRole` as the gate would therefore false-negative `hr_admin` (locked out of sync when Web allows it) and false-positive `chairperson` / `team_lead` (granted when Web's manager-or-above role set does not include them — Web's `isAdmin` helper grants chairperson separately via a higher tier).
    - Solution: `ProjectDetailViewModel.canSyncRiskAction(role: String?) -> Bool` checks the raw role string lower-cased against `syncEnabledRoles: Set<String> = ["super_admin", "admin", "hr_admin", "manager"]` — Web's exact role set, verbatim. Recorded as a deliberate iOS divergence from the `PrimaryRole` helper because Web's role set does not cleanly map onto the iOS primary-role enum.
    - View helper `canSyncRiskAction` reads `sessionManager.currentProfile?.role` raw and routes through the VM's static gate. Button is disabled (gray capsule) + a "Converting a risk into a risk action requires admin or manager privileges." hint renders when the check fails.

  - **Goal C — Foundation data struct + isolated state**:
    - `Brainstorm+/Features/Projects/ProjectDetailModels.swift` (modified): new public `RiskActionSyncDraft: Equatable { title: String, detail: String, severity: String }`. Confirmation-preview payload built at tap-time. Docstring records persistence parity (Chinese literals `风险项` in title + `从项目风险分析生成` in `suggested_action` survive into the database exactly as Web writes them).
    - `Brainstorm+/Features/Projects/ProjectDetailViewModel.swift` (modified): new public `RiskActionSyncPhase: Equatable { case idle, syncing, succeeded }` (no `.failed` — scoped `riskActionSyncErrorMessage` alone signals failure, mirroring 2.4 / 2.5 posture) + three `@Published` properties (`riskActionSyncPhase`, `riskActionSyncErrorMessage`, `lastSyncedRiskActionId`). `applyDeniedState()` extended to reset all three.

  - **Goal D — Write flow (Path A direct insert)**:
    - `ProjectDetailViewModel.syncRiskActionFromDetail(rawRole: String?) async -> Bool`:
      1. **RBAC gate** via `canSyncRiskAction(role:)` — fails fast with a scoped error before any Supabase contact.
      2. **Precondition re-check** (`project != nil && riskAnalysis != nil`) survives a race with `applyDeniedState()`.
      3. **Draft rebuild** inside the VM (not taken from the View) so a stale view snapshot cannot poison the insert. `trimmingCharacters(.whitespacesAndNewlines)` on title with empty-title defense (mirrors Web's line-327 trim-and-reject).
      4. Flip `riskActionSyncPhase = .syncing`, clear `riskActionSyncErrorMessage`.
      5. **Step 1 — anchor re-fetch**: `project_risk_summaries.select("id").eq("project_id", value: projectId).limit(1).execute().value` decoded as `[LinkedRiskAnchorRow]` (reused from 2.4 / 2.5 — same shape). Zero rows → scoped error "No risk analysis exists for this project yet. Generate one on the web first." + phase revert to `.idle` + return false.
      6. **Step 2 — auth + org resolution**: `client.auth.session.user` for `user.id`; `profiles.select("org_id").eq("id", value: user.id).limit(1).execute().value` decoded as `[ProfileOrgRow { orgId: UUID }]` private DTO. Missing user / org → scoped error + phase revert + return false.
      7. **Step 3 — primary insert**: `client.from("risk_actions").insert(payload).select("id").single().execute().value` decoded as `[RiskActionInsertResult { id: UUID }]`. `RiskActionInsert: Encodable` private DTO carries `org_id, risk_type: "manual", source_type: "project", source_id: project.id, ai_source_id: anchor.id, title: trimmedTitle, detail: sliced.nilIfEmpty, severity: mappedSeverity, suggested_action: "从项目风险分析生成", status: "open", created_by: user.id`. CodingKeys map camelCase → snake_case.
      8. **Step 4 — best-effort audit log**: `risk_action_events` insert with `action_id, event_type: "created", to_status: "open", actor_id`. Wrapped in inner `do/catch` + silent swallow on failure (mirrors Web's `logRiskEvent` resilience).
      9. **Post-success**: set `lastSyncedRiskActionId = inserted.id` + `riskActionSyncPhase = .succeeded`. Dispatch `refreshLinkedRiskActions()` + `refreshResolutionFeedback()` as fire-and-forget parallel tasks (neither awaited — each owns its own error rail).
      10. **Failure posture**: only `riskActionSyncErrorMessage` is written; phase reverts to `.idle`; `lastSyncedRiskActionId` preserved across failure so a prior successful sync stays discoverable. Does NOT touch `errorMessage`, `enrichmentErrors`, `deleteErrorMessage`, `summaryErrorMessage`, `riskAnalysisErrorMessage`, `linkedRiskActionsErrorMessage`, `resolutionFeedbackErrorMessage`.
    - Helper `ProjectDetailViewModel.mapRiskLevelToSeverity(_:)` pre-maps `ProjectRiskAnalysis.RiskLevel` → severity string (critical/high → "high", medium → "medium", low → "low", unknown → "medium"). Public helper `riskActionSyncDraft()` returns the view-facing preview; returns `nil` when `project == nil || riskAnalysis == nil`.
    - `clearRiskActionSyncSuccess()` auto-invoked 3s after success by the View (mirrors Web's `setTimeout(() => setSyncMsg(null), 3000)` at page.tsx:221); VM guards against clearing a newer in-flight sync.

  - **Goal E — UI affordance with confirmation dialog**:
    - `Brainstorm+/Features/Projects/ProjectDetailView.swift` (modified):
      - Two new `@State` properties: `isShowingRiskActionSyncConfirm: Bool = false` + `pendingRiskActionDraft: RiskActionSyncDraft? = nil` (snapshotted at tap-time so a concurrent `fetchDetail()` refresh can't mutate the preview mid-confirmation).
      - Two new computed properties: `rawRole: String?` (raw session role straight-through) + `canSyncRiskAction: Bool` (VM static helper).
      - New `@ViewBuilder riskActionSyncAffordance` inside `riskAnalysisSection`, rendered only when `viewModel.riskAnalysis != nil`. Layout: warning-tinted capsule button "Convert to risk action" → disabled + gray capsule + "Converting a risk into a risk action requires admin or manager privileges." hint when role fails gate / mid-sync / delete in flight. `ProgressView` + "Converting…" label while `.syncing`. Scoped error row via `summaryErrorRow(message:)` when `riskActionSyncErrorMessage` set. Green success banner ("Risk action created and linked to this analysis.") when `.succeeded`.
      - `.confirmationDialog("Convert risk analysis to risk action?", isPresented: $isShowingRiskActionSyncConfirm, presenting: pendingRiskActionDraft)` attached to the `riskAnalysisSection` card. Dialog message lists title + severity label + detail preview so the user sees exactly what will be persisted. "Create risk action" confirm → `await viewModel.syncRiskActionFromDetail(rawRole: rawRole)`; if success, `scheduleRiskActionSyncSuccessClear()` spawns a 3s `Task` that calls `viewModel.clearRiskActionSyncSuccess()`. Cancel clears `pendingRiskActionDraft` without touching VM state.
      - `linkedRiskActionsSection` subtitle copy updated from "Read-only · converting a risk into an action is only available on the web." → "Linked to the current risk analysis. Convert a new risk action above; resolution write-backs (close / reopen / effectiveness) remain on the web for now." — reflects the new write affordance honestly without overclaiming.
      - `foundationScopeNote` copy updated from "Converting risks into actions and generating new risk analyses from iOS are available on the web and will arrive in later iOS rounds." → "Generating fresh risk analyses and writing resolution outcomes (close / reopen / effectiveness) remain on the web and will arrive in later iOS rounds." — removes the "converting risks into actions" carve-out (now shipped) while keeping generate-from-iOS + resolution write-back honest.

  - **Goal F — Persistence parity**:
    - `title` literal mirrors Web's template exactly: `"[\(project.name)] 风险项"` — Chinese `风险项` suffix persisted so rows written from iOS look identical to Web-written rows in `risk_actions.title`. UI dialog copy stays English-only; this is a persistence-level string, not a user-facing one.
    - `suggested_action` literal mirrors Web's hardcoded value: `"从项目风险分析生成"` — identical persisted text regardless of origin-of-write.
    - `risk_type: "manual"`, `status: "open"`, `source_type: "project"` all hardcoded to match Web's call-site defaults.
    - `severity` mapping ternary mirrors Web's exact branches (critical/high → high, medium → medium, low → low) with iOS-specific `.unknown → "medium"` branch (Web never constructs the button with an unknown level; neutral default chosen over under-/over-stating).

  - **Goal G — Scope control**: no risk-action update / close / reopen / effectiveness write, no governance note write, no bulk convert, no assignee picker, no due-date picker, no risk-action detail modal, no full risk-action management module, no generate-risk-from-iOS, no Web code changes, no migration / RLS changes, no new HTTP endpoint, no new Edge Function, no `api_keys` touchpoint, no embedding of provider credentials on-device, no LLM narrative on risk analysis, no i18n refactor, no visual redesign, no auto-fetch-on-detail-load. `.projects` remains `.partial`.

  - **Goal H — Ledger truth**:
    - `progress.md` appended with the 2.6 delivery block; Active now points to Winston 2.6 audit.
    - `findings.md` to be updated (same pass) with deltas flipped to Delivered (2.6) for risk-action sync + RBAC-gated button + confirmation dialog + 3s auto-clear + post-write refresh + best-effort audit event.
    - `task_plan.md` marks `2.6 Projects Risk Action Sync Write Path Foundation completed` and next-step Winston 2.6 audit.
    - New `docs/parity/49-winston-ready-2.6-notes.md` Winston-ready notes.

  - `AppModule.projects` remains `.partial` — resolution write-back (close / reopen / effectiveness / governance note), generate-risk-analysis-from-iOS, LLM narrative on summary, and the full risk-action management surface are all still missing. 2.6 closes the write-path foundation gap (single affordance, confirmation-gated, role-gated) but does not promote to `.full`.
  - Independent build passed (`** BUILD SUCCEEDED **`) on `iPhone 17 Pro Max` destination with `CODE_SIGNING_ALLOWED=NO`.
  - Verification scan count **78 across 3 files** matching `'riskActionSync|RiskActionSync|canSyncRiskAction|syncRiskActionFromDetail|RiskActionInsert|RiskActionEventInsert|mapRiskLevelToSeverity|clearRiskActionSyncSuccess|syncEnabledRoles|RiskActionSyncDraft|RiskActionSyncPhase|lastSyncedRiskActionId'` inside `Brainstorm+/Features/Projects` (`ProjectDetailModels.swift: 2`, `ProjectDetailViewModel.swift: 53`, `ProjectDetailView.swift: 23`).

- **Tools/Skills**: `planning-with-files` (scoped plan before any code edit — Web re-read first, iOS audit of RBAC + Profile second, Path A vs Path B decision third, implementation fourth, ledger fifth). `verification-before-completion` (devprompt §7.A scan + build expectation met; BUILD SUCCEEDED captured; scan count 78 across 3 files). `systematic-debugging` (Explore-agent deep read of `BrainStorm+-Web/src/lib/actions/risk-actions.ts` + dashboard page call site + RBAC helper + role-set migration + RLS migration — critical input for the Path A decision and for the raw-string RBAC check). `receiving-code-review` (2.5.1 minimum-fix sprint immediately preceded this round with a governance-priority fix; 2.6 kept disciplined scope discipline consistent with the audit posture). `clarify` (no clarification round needed — devprompt spelled out UI scope, state isolation, persistence parity, scope prohibitions unambiguously). `supabase-postgres-best-practices` substituted by in-repo SDK pattern review — insert + `.select("id").single()` return pattern, Encodable DTO with snake_case CodingKeys, best-effort audit log wrapped in inner `do/catch`. `ios-development-best-practices` skill file not present under `~/.claude/skills/`; substituted by in-repo SwiftUI patterns + devprompt §3 scope discipline — `@MainActor ObservableObject`, three-phase enum + side-band error, confirmation-dialog presenting a snapshotted draft, fire-and-forget post-write refresh tasks (non-awaited, each owns its own error rail). `native-data-fetching` — two-step anchor-then-fetch-then-write with preservation of prior snapshot on transient failure; auth user + org resolution inline inside the write method rather than persisted on `Profile` (minimal blast radius).
- 2.6 prompt created: `devprompt/2.6-projects-risk-action-sync-write-path-foundation.md`.

## Winston 2.6 Audit — 2026-04-19

- **Verdict**: PASS (with 3 non-blocking documentation corrections). Full report: `docs/parity/50-winston-audit-2.6.md`.
- **Independent re-verification** of sprint 2.6 conducted without trusting ready-notes self-reports. Source-of-truth re-reads: `ProjectDetailModels.swift` / `ProjectDetailViewModel.swift` / `ProjectDetailView.swift` / `AppModule.swift` on iOS side; `risk-actions.ts` / `page.tsx` / `rbac.ts` on Web side.
- **Spec compliance**: 8/8 devprompt §7 clauses confirmed (Path A direct INSERT, confirmation dialog, isolated state machine, post-write refresh, best-effort audit event, client RBAC gate, literal persistence parity `[X] 风险项` + `从项目风险分析生成`, zero scope creep).
- **Field parity**: 11-column `risk_actions` INSERT + 4-column `risk_action_events` event table — every field aligned with Web byte-for-byte on literals, semantically equivalent on derived values.
- **Build verification**: `xcodebuild build -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` → `** BUILD SUCCEEDED **`. (Simulator destination unavailable locally due to CoreSimulator 1051.49.0 vs build 1051.50.0 mismatch — generic iOS is functionally equivalent for compile-level parity verification.)
- **Identified doc errors in 49-ready-notes** (recorded in audit §8 for next sprint to fix):
  1. Path `BrainStorm+-Web/src/lib/security/rbac.ts` does not exist; real path is `BrainStorm+-Web/src/lib/rbac.ts`.
  2. Claim that `serverGuard({requiredRole:'manager'})` resolves to `{super_admin, admin, hr_admin, manager}` is **wrong** — `ROLE_LEVEL` map has no `hr_admin`; level≥2 resolves to `{manager, team_lead, admin, super_admin, superadmin, chairperson}`. iOS's whitelist actually mirrors **DB RLS policies (migrations 014 / 037)**, which is strictly narrower than Web server guard but is the authoritative write-time authority.
  3. iOS `lastSyncedRiskActionId: UUID?` is set but has no View consumer yet — acceptable as intentional extension point for 2.7 resolution write-back, not dead code.
- **Recommended next step**: Sprint 2.7 Resolution Write-Back MVP (`risk_actions.UPDATE` for status/effectiveness/resolution_category/reopen), during which `canSyncRiskAction` should be refactored into `RBACManager.canManageRiskActions(profile:)` routing through `PrimaryRole` + capability — this retires the raw-string whitelist debt and unblocks Phase-2 3-role migration.

## Sprint 3.0 RBAC Infrastructure Web-Parity Foundation
- Devprompt: `devprompt/3.0-rbac-infrastructure-web-parity-foundation.md`. Ready-notes: `docs/parity/51-winston-ready-3.0-notes.md`.
- **Scope**: Pure RBAC/auth plumbing — no feature work. Goal was to align iOS's 4-role enum + 26-capability set + legacy-role handling + missing `excluded_capabilities` with Web's canonical Phase 2 3-role + 30-capability + excluded-subtract model (`BrainStorm+-Web/src/lib/capabilities.ts` + `src/lib/role-migration.ts`).
- **Deltas landed** (9 Tasks A–I):
  - **Task A**: `PrimaryRole` loses `.chairperson` (Phase 2 cleanup). `PrimaryRole` is now exactly `{ employee, admin, superadmin }`. Legacy `chairperson` string folds to `.superadmin` in `migrateLegacyRole` with a deprecation warning flagging migration-049 drift.
  - **Task B**: `Capability` gains 4 cases — `attendance_admin`, `leave_quota_admin`, `ai_evaluation_access`, `holiday_admin` — inserted before system-governance caps to match Web's `src/lib/capabilities.ts:41-45` grouping. Total capabilities now 30, exhaustive with Web.
  - **Task C**: `defaultCapabilities` rewritten. `admin = 18 caps` (HR package + base approvals + admin-as-fallback approver + AI assistant + 管理扩展). `superadmin = 30 caps` (every approval cap + every AI cap + every functional cap + every governance cap + every 管理扩展 cap). Counts pinned in tests. **Note**: devprompt's "superadmin = 29" was stale vs. Web source-of-truth — Web has 30 caps at head. iOS mirrors Web, not the devprompt.
  - **Task D**: `migrateLegacyRole(_:)` gains `logLegacyRoleWarning(_:mappedTo:)` helper. Canonical roles (`superadmin` / `admin` / `employee`) pass through silently. Legacy roles (`chairperson` / `super_admin` / `manager` / `team_lead` / `hr` / `finance` / `contractor` / `intern`) map + log a `⚠️ [role-migration]` warning. Unknown roles default to `.employee` + separate warning. Result: migration-049 drift (profile rows the migration should have normalized but missed) now produces visible telemetry instead of silent pass-through.
  - **Task E**: `Profile` model adds `excludedCapabilities: [String]?` with JSON key `excluded_capabilities`. Matches Web `profiles.excluded_capabilities` JSONB column (Supabase migration 059). Automatic Codable synthesis — no call-site `Profile(...)` updates needed (all existing sites use synthesized memberwise init via decoder).
  - **Task F**: `getEffectiveCapabilities(for:)` rewritten to match Web spec: `(defaults ∪ migration-derived ∪ DB-assigned) − excluded`. Excluded subtraction is the new Phase-2 primitive that lets an admin strip specific caps from an individual user (e.g., revoke `holiday_admin` without demoting role).
  - **Task G**: `RBACManager` gains `canManageRiskActions(rawRole:)` + `canManageRiskActions(profile:)`. DB-RLS-mirror whitelist (`['super_admin', 'superadmin', 'admin', 'hr_admin', 'manager']`) now lives in one place. `ProjectDetailViewModel.canSyncRiskAction(role:)` + `syncEnabledRoles` deleted. `ProjectDetailView.canSyncRiskAction` computed var delegates to the shared manager. Design choice: kept as raw-string whitelist rather than `PrimaryRole` + capability lookup because `migrateLegacyRole` intentionally drops `hr_admin` (DB RLS accepts it; iOS's canonical 3-role model doesn't have a slot for it).
  - **Task H**: `docs/parity/49-winston-ready-2.6-notes.md` §2.3 + §2.4 RBAC passage re-grounded. Path corrected to `src/lib/rbac.ts` (was `src/lib/security/rbac.ts`, which does not exist). serverGuard role set corrected to 6-role (`manager` / `team_lead` / `admin` / `super_admin` / `superadmin` / `chairperson` at level ≥ 2). iOS 4-role whitelist re-contextualized as DB-RLS mirror (migrations 014 + 037), intentionally narrower.
  - **Task I**: Ledger trio synced (this update). Winston ready-notes authored at `docs/parity/51-winston-ready-3.0-notes.md`.
- **Verification** (devprompt §4 checklist):
  - `xcodebuild build -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` → `** BUILD SUCCEEDED **`.
  - `xcodebuild test -only-testing:"Brainstorm+Tests/RBAC_Sprint30_Tests"` → 7/7 green: `defaultCapabilitiesAdminCount` (18 ✓), `defaultCapabilitiesSuperadminCount` (30 ✓), `migrateLegacyChairpersonFoldsToSuperadmin` (.superadmin ✓), `migrateLegacyHrDerivesCapabilities` (.employee + hr_ops + ai_resume_screening ✓), `profileDecodesExcludedCapabilities` (JSON round-trip ✓), `effectiveCapabilitiesSubtractsExcluded` (`excluded: [holiday_admin]` removes from admin default ✓), `riskActionsGateMirrorsDbRls` (5-member whitelist + rejects employee / nil ✓).
- **Consumer sites updated**:
  - `ProjectListViewModel.isAdmin(role:)` — drops `.chairperson` from switch.
  - `ProjectDetailViewModel.isAdmin(role:)` — drops `.chairperson` from switch.
  - `ProjectDetailViewModel` — deletes local `canSyncRiskAction(role:)` + `syncEnabledRoles`; `syncRiskActionFromDetail(rawRole:)` gate now calls `RBACManager.shared.canManageRiskActions(rawRole:)`.
  - `ProjectDetailView.canSyncRiskAction` computed property — delegates to shared manager; doc comment re-grounded.
- **Why 3.0 blocks XL-tier feature work**: Parity table Task #12 (Auth/RBAC) is the foundation under every other module. 3.1 Chat gates on `ai_chatbot_access`, 3.2 Dashboard templates gate on primary role + capability flags, 3.3 Attendance gates on `attendance_admin` + attendance rules, 3.4 Reporting gates on `daily_log_approval` / `weekly_report_approval`, 3.5 Copilot gates on multiple AI caps. Running any of them on the pre-3.0 26-capability model would bake in drift that would have to be ripped out later.

## Sprint 3.1 Team Chat Foundation — 2026-04-21
- Devprompt: `devprompt/3.1-team-chat-foundation.md`. Ready-notes: `docs/parity/52-winston-ready-3.1-notes.md` (authored under Task G).
- **Scope**: First usable Team Chat surface on iOS — channel list (Web-parity access-filtered), message read (`created_at ASC limit 50`), message send (insert + optimistic append + best-effort `chat_channels` tail update), and Supabase Realtime v2 `INSERT` subscription. Attachments / reactions / withdraw / reply threading / mentions / search / create-conversation / DM find-or-create / AI Copilot explicitly deferred per devprompt §6.
- **Deltas landed** (6 Tasks A–F; Task G ready-notes authored after; Task D subagent committed first, Tasks A/B/C filled in afterward — chronological landing order A → B/C → D):
  - **Task A**: `Brainstorm+/Core/Models/ChatModel.swift` adds `ChatChannelMember: Identifiable, Codable, Hashable` struct + nested `MemberRole` enum (`owner` / `admin` / `member`) with `channel_id` / `user_id` / `joined_at` snake-case CodingKeys. `ChatMessage` intentionally unchanged — `attachments` / `reactions` / `reply_to` still deferred per 3.1 scope exclusion.
  - **Task B**: `Brainstorm+/Features/Chat/ChatListViewModel.swift` rewritten to mirror Web `getAccessibleChannelMap` (`src/lib/actions/chat.ts:252-282`). Three parallel `async let` queries — announcement channels (`type='announcement'`), owned channels (`created_by=currentUserId`), membership anchor rows (`chat_channel_members.user_id=currentUserId` → resolve `channel_id` set → second `.in("id", ...)` query). Set-based dedupe by channel id. Sort: `lastMessageAt DESC nulls last`, `createdAt` tiebreaker. Blind `SELECT * FROM chat_channels` deleted (RLS `USING (true)` made that a cross-user data leak).
  - **Task C**: `Brainstorm+/Features/Chat/ChatRoomViewModel.swift` (new) — `@MainActor ObservableObject` with 5 methods. `bootstrap()` does the access gate (announcement OR `createdBy == userId` OR membership row exists, else `accessDenied = true`) before any fetch or subscribe, mirroring Web `ensureChannelAccess` (chat.ts:284-305). `fetchMessages()` pulls `created_at ASC limit 50` matching Web `fetchMessages` (chat.ts:539-576). `sendMessage(_:)` does insert → returned row optimistically appended (dedup by id guards against Realtime echo) → best-effort `chat_channels.last_message` / `last_message_at` UPDATE via migration 026 policy (`auth.uid() IS NOT NULL`). `subscribeRealtime()` uses Supabase Swift SDK v2 `postgresChange(InsertAction.self, schema: "public", table: "chat_messages", filter: .eq("channel_id", channel.id))`, with `JSONObject.decode(as:)` inheriting `JSONDecoder.supabase()` fractional-second ISO8601 decoding. `teardown()` cancels the realtime task and unsubscribes; `deinit` only cancels the task (MainActor-isolated `teardown()` is not callable from deinit), so `ChatRoomView` must call `teardown()` from `.onDisappear`.
  - **Task D**: `Brainstorm+/Features/Chat/ChatRoomView.swift` rewritten to bind to `ChatRoomViewModel` via `@StateObject(wrappedValue:)`. `.task { await viewModel.bootstrap() }` + `.onDisappear { viewModel.teardown() }`. Message bubbles render `isCurrentUser = msg.senderId == viewModel.currentUserId`; ScrollViewReader auto-scrolls on `messages.count` change. Three-state UI: `accessDenied` rail / loading-empty rail / normal list. Send button disabled when `isSending || trimmed.isEmpty`; input cleared only after `sendMessage` returns (not optimistically).
  - **Task E**: `Brainstorm+/Features/Chat/ChatDateFormatter.swift` (new) — `enum ChatDateFormatter` with `static func format(_ date: Date?) -> String`. Four branches: nil → `""`, today → `HH:mm`, yesterday → `昨天 HH:mm`, this-year-earlier → `M月d日 HH:mm`, older → `yyyy年M月d日`. `zh_CN` locale, static-cached `DateFormatter` instances. Mirrors Web page.tsx inline formatter with iOS-idiomatic labels.
  - **Task E cont.**: `Brainstorm+/Features/Chat/ChatListView.swift` patched — NavigationLink destination now constructs `ChatRoomView(viewModel: ChatRoomViewModel(client: supabase, channel: channel))`; list-cell `Text(date, style: .time)` replaced with `Text(ChatDateFormatter.format(channel.lastMessageAt))`. No new FAB / search / unread-badge surfaces (all five remain in 3.1-debt-04).
  - **Task F**: Ledger trio synced (this update).
- **Verification**:
  - Build: `xcodebuild build -destination "generic/platform=iOS Simulator"` Debug with `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` → `** BUILD SUCCEEDED **`.
  - Commits on `main`:
    - `fdaf782` docs(devprompt): add sprint 3.1 team chat foundation plan
    - `0e6e456` feat(chat): add ChatChannelMember model for membership access checks
    - `f591231` feat(chat): replicate web access gate and realtime stream in chat viewmodels
    - `7b0b135` feat(chat): bind ChatRoomView to ChatRoomViewModel with realtime teardown
- **Debt carry-forward (7 open after closeout pass; full diagnosis in `52-winston-ready-3.1-notes.md`)**:
  - **3.1-debt-01 Attachments / images / files — ✅ CLOSED (image + file subset) 2026-04-21** via Sprint 3.3 (App commit `93ad38f`): `ChatMessage` gained `attachments: [ChatAttachment]` with null-tolerant decoder, ViewModel `uploadAttachment` writes to `chat-files/{user_id}/{channel_id}/{uuid}.{ext}` (user-JWT satisfies existing RLS without admin client), View renders AsyncImage 200×200 / file icon+name rows, both wrapped in `Link` for tap-to-open. Still deferred: image compression, signed-URL reads (bucket is public), upload progress bar, inline quick-look preview — all tracked as 3.3-followup.
  - **3.1-debt-02 Message withdraw**: `is_withdrawn` / `withdrawn_at` on the model but no UI path, no server-side RLS UPDATE test. Web has the flow; iOS lags.
  - **3.1-debt-03 Reply threading**: `reply_to: UUID?` present on model, not consumed at render. Web `fetchMessages` does reply-to second query; iOS skips per devprompt §6 scope exclusion. 3.2+.
  - **3.1-debt-04 Mentions / search / create-conversation / find-or-create-DM** (partially closed 2026-04-21 via Sprint 3.2): create-conversation + find-or-create-DM ✅ CLOSED — iOS `ChatListView` now has toolbar `+` entry wrapping `NewConversationSheet` + `UserPickerView`, calling two new SECURITY DEFINER RPCs (`chat_find_or_create_direct_channel`, `chat_create_group_channel`) added in Web migration `20260421130000_chat_conversation_rpc.sql`. Race-safety via existing partial unique index on `chat_channels(participant_pair_key) WHERE type='direct'` (INSERT loser catches `unique_violation` + re-SELECT). Selected RPC-over-API-route architecture: single DB-side source of truth (Web server actions can migrate to call these later), full auth + validation + dedup enforced server-side, group role gating deferred to UI layer matching Web's current posture. 9-assertion native-PG harness green (`/tmp/chat_rpc_test.sh` — first-create, fast-path, symmetric call, self-reject via `invalid_parameter_value`, unauth-reject via `insufficient_privilege`, group happy path, empty name reject, zero-member reject, dedup). Web commit `08bce19`, App commit `7143142`. Still deferred: `@mentions` (Web parses `content.includes('@<name>')` + writes `chat_mentions`; iOS text-only), message search (Web `ilike` on `chat_messages.content`).
  - **3.1-debt-05 Web RLS tightening (system-level security debt)**: Sprint 3.2a IN PROGRESS. Task A ✅ (`BrainStorm+-Web/supabase/migrations/20260421000000_chat_rls_tightening.sql` replaces `USING (true)` on `chat_channels` + `chat_messages` SELECT with the three-way union announcement ∪ owned ∪ member-of). Task B ✅ (`docs/parity/54-3.2a-task-b-web-regression-audit.md`: 23 admin reads + 2 user-role Realtime reads classified, zero pre-migration code changes required). Task C1 ✅ (local RLS semantic verification via native Postgres + `auth.uid()` stub; `docs/parity/55-3.2a-task-c1-local-verification.md` records 4-user scenario matrix all green). Remaining: C2 iOS staging smoke, C3 staging shadow ≥24h, Task D (optional iOS client-side gate simplification), Winston 3.2a re-audit.
  - **3.1-debt-06 Realtime connection failure visibility — ✅ CLOSED** in closeout pass. `subscribeWithError()` writes `errorMessage` on throw (commit `06b7f99`); `ChatRoomView` surfaces via `.zyErrorBanner($viewModel.errorMessage)` (new `Shared/DesignSystem/Modifiers/ZYErrorBannerModifier.swift`).
  - **3.1-debt-07 Chat error surfacing — ✅ CLOSED** in closeout pass. Same shared `ZYErrorBannerModifier` attached to both `ChatRoomView` and `ChatListView`; fetch/send/list failures render as top banner with 5s auto-dismiss + manual close.
  - **3.1-debt-08 Nav-destination laziness**: `ChatListView` NavigationLink eagerly builds `ChatRoomViewModel` per row; migrate to `NavigationLink(value:)` + `.navigationDestination(for:)`.
  - **3.1-debt-09 Mock-Supabase testability harness**: zero automated coverage on chat viewmodels — access-gate union, empty result, decode failure, send failure, realtime decode/subscribe failure all manual-only. Blocker is `SupabaseClient` has no protocol seam. Split out from debt-05 in closeout pass (originally folded together) so "add tests" stops coupling to "tighten RLS." Revisit first sprint that adds non-trivial chat codepath or budgets testability pass; intro minimal `ChatDataSource` protocol.
- **Why 3.1 matters**: First Realtime usage in the iOS codebase (Supabase Swift SDK v2 `postgresChange` + `InsertAction` + `.eq` filter + `JSONObject.decode(as:)` inheriting `JSONDecoder.supabase()` fractional-second ISO8601 handling). Pattern becomes the reference for future Realtime surfaces (Notifications, Activity Feed, live approvals). Also establishes the client-side-access-gate pattern for every module where Web bypasses RLS via admin client — chat is the first of several.

## Active
- 3.3 Chat Attachments (image + file) — ✅ CLOSED on App side (Winston 3.3 + staging smoke batched to unified testing phase per standing directive):
  - **Goal**: close 3.1-debt-01 image/file subset on iOS so 1:1 with Web's `chat-files` upload + JSONB attachments render. Signed URLs / HEIC compression / upload-progress UI explicitly deferred.
  - **Source-of-truth scan**: `BrainStorm+-Web/src/lib/actions/chat.ts:5-10` `ChatAttachment` interface `{name,url,type,size?}`; `chat.ts:578-654` `sendMessage` inserts into `chat_messages` with JSONB `attachments` + derives `type` from `attachments.length>0 ? (all images→'image' : 'file') : 'text'`; `chat.ts:169-185` `normalizeAttachments` tolerates null/missing JSONB; production upload route is `src/app/api/chat/upload/route.ts:9` which uses bucket `'chat-files'` via `createAdminClient()` (service_role bypasses storage RLS) — the `page.tsx:288` reference to `'chat_attachments'` is dead code (bucket doesn't exist; unreachable client branch). Migration `028_chat_message_features.sql:38-59` creates public `chat-files` bucket with RLS `auth.uid()::text = (storage.foldername(name))[1]` (first path segment must be user_id).
  - **Architecture choice (autonomous)**: iOS can't mirror Web's admin-client bypass, so upload via **user-JWT direct to storage** with path `{user_id}/{channel_id}/{uuid}.{ext}` — first segment satisfies existing RLS policy, channel segment preserves archival semantics, UUID+ext prevents collisions. Public bucket → reads use `getPublicURL(path:)` with no signed-URL TTL management. Chose this over (A) calling Web's `/api/chat/upload` route (adds Web-uptime dependency) or (C) SECURITY DEFINER RPC (unnecessary for storage-level auth).
  - **Model** (`Brainstorm+/Core/Models/ChatModel.swift`): new public `ChatAttachment: Codable, Hashable { name, url, type (MIME), size: Int? }` + `isImage: type.hasPrefix("image/")`. `ChatMessage` gains `attachments: [ChatAttachment]`. Custom `init(from decoder:)` wraps attachments decode in `try? c.decodeIfPresent(...) ?? []` to tolerate legacy rows or Realtime payloads where field may be absent/null — matches Web's `normalizeAttachments` fallback.
  - **ViewModel** (`Brainstorm+/Features/Chat/ChatRoomViewModel.swift`): `sendMessage(_:attachments:)` now accepts `[ChatAttachment] = []`, drops the empty-text guard (empty text allowed when attachments non-empty — matches Web's `content` posture), derives `type` via `attachments.isEmpty ? "text" : (allSatisfy({ $0.isImage }) ? "image" : "file")`. `InsertPayload` gains `attachments: [ChatAttachment]`. `last_message` preview mirrors Web: trimmed text if present, else `"[图片]"` / `"[文件]"`. New `uploadAttachment(data:fileName:mimeType:)` uploads via `client.storage.from("chat-files").upload(path:, data:, options: FileOptions(contentType:, upsert: false))` then returns `ChatAttachment(name, getPublicURL(path:).absoluteString, mimeType, data.count)`.
  - **View** (`Brainstorm+/Features/Chat/ChatRoomView.swift`): `+` button replaced with `Menu` wrapping `PhotosPicker(selection: $photoItems, maxSelectionCount: 9, matching: .images)` and `Button → showFileImporter = true`. `.onChange(of: photoItems)` consumes `PhotosPickerItem.loadTransferable(type: Data.self)` and pushes `PendingUpload(data, fileName: "IMG_<uuid>.jpg", mimeType: "image/jpeg", previewImage: UIImage(data:))` into `pendingUploads`. `.fileImporter(allowedContentTypes: [.item], allowsMultipleSelection: true)` pairs with `startAccessingSecurityScopedResource()` + `Data(contentsOf:)` + `UTType(filenameExtension:)?.preferredMIMEType` MIME derivation. Horizontal `pendingAttachmentsStrip` above input bar renders 64×64 thumbnails (UIImage for images, `doc.fill` + filename for files) with `xmark.circle.fill` remove chip. `sendTapped` awaits each `uploadAttachment` sequentially, collects into `[ChatAttachment]`, then calls `sendMessage(text, attachments:)`; upload failure surfaces via `errorMessage` banner and does NOT consume attachments. `messageBubble` grows: `msg.isWithdrawn → "此消息已撤回"` italic gray bubble with no attachment render; else text bubble (skipped when content empty to avoid a blank bubble over attachments-only messages) + `ForEach(attachments)` rendering `AsyncImage` 200×200 rounded for images or `doc.fill + filename` row for files, both wrapped in `Link(destination: URL)` so tap opens in Safari / system quick-look.
  - **Build**: `xcodebuild build -destination "generic/platform=iOS Simulator" -configuration Debug` → **BUILD SUCCEEDED**. SourceKit inline diagnostics complained about missing types (Supabase / UIKit / ChatAttachment / etc.) — all false positives from the VSCode indexer running out of Xcode's SPM graph; the real compiler accepted everything.
  - **Commits**: App `93ad38f` (+414 / -24 across 3 files). No Web or DB changes this sprint — the chat-files bucket + RLS + `attachments` JSONB field all pre-existed from migration 028.
  - **Deferred (explicit)**: (1) image compression before upload (Web uploads raw too), (2) signed-URL reads (bucket is public), (3) upload progress bar (Web has spinner only), (4) multi-image gallery modal on tap (Web just opens in new tab — parity), (5) file quick-look preview inline (parity: Web also delegates to browser / OS), (6) attachment size limit / extension whitelist (no server-side enforcement in Web either). (7) 1:1 Winston 3.3 audit + staging shadow smoke batched into the final unified testing phase per standing directive "先做 1:1 移植和 app 前端，最后统一测试."
- 3.1 closeout pass: Winston audit PASS-with-conditions returned three conditions — (1) three ledger failures in `52-winston-ready-3.1-notes.md` (stale mock-Supabase cross-ref, stale subscribe/teardown descriptions), (2) error feedback UI not closed (debt-06/07), (3) SELECT RLS remains system-level security debt (debt-05). Closeout resolved 1+2 inside this session: three ledger failures corrected in the ready-notes; shared `ZYErrorBannerModifier` authored under `Shared/DesignSystem/Modifiers/` and wired into both `ChatRoomView` + `ChatListView`; `xcodebuild` green. Debt-05 converted from vague "architectural" debt into concrete spec `devprompt/3.2a-chat-rls-tightening.md` for a cross-stack security sprint. Debt-09 split out of debt-05 to stop coupling "add tests" to "tighten RLS."
- 3.2a Chat RLS Tightening in progress (autonomous execution under repeated "你自己决定" delegation):
  - **Task A ✅**: migration `BrainStorm+-Web/supabase/migrations/20260421000000_chat_rls_tightening.sql` — DROPs `"Anyone can view channels"` / `"Users can view messages"` (both `USING (true)`), CREATEs `chat_channels_select_membership` (three-way union: `type = 'announcement' OR created_by = auth.uid() OR EXISTS (chat_channel_members)`), CREATEs `chat_messages_select_membership` (containing-channel visibility via `EXISTS` against the new chat_channels predicate). Wrapped in BEGIN/COMMIT. 77 lines including scope preamble + debt-10 annotation. Filename uses timestamp convention (not serial 029) matching latest migration `20260418110000`.
  - **Task B ✅**: three parallel Explore subagents audited Web chat read paths; consolidated `docs/parity/54-3.2a-task-b-web-regression-audit.md` confirms 23 admin-client reads in `src/lib/actions/chat.ts` unaffected (service_role bypasses RLS), 1 filtered user-role Realtime read (`dashboard/chat/page.tsx:200` channel_id-filtered, already RLS-compatible), 1 global user-role Realtime read (`notification-provider.tsx:81-88` `@mention` listener) narrows to member-channels — **strict improvement** (eliminates dead-link notifications for non-member channels). Zero pre-migration code changes required. Debt-10 (membership row leak via `chat_channel_members` SELECT = `auth.uid() IS NOT NULL`) surfaced as out-of-scope follow-up.
  - **Task C1 ✅ (approximation)**: Docker daemon unavailable for `supabase start`, so verified via native Postgres 15 + `auth.uid()` stub (session GUC `request.jwt.claim.sub`). `/tmp/chat_rls_test.sh` applied migration verbatim, seeded 3 channels × 4 users, ran programmatic assertions. All four scenarios green: User A (DM member) → announce + A-B DM, User B (DM member) → announce + A-B DM, User C (C-team owner) → announce + C team, Stranger D → announce only (0 non-announcement leaked). Intermediate `SET LOCAL` harness bug caught; post-fix re-run all green. Evidence: `docs/parity/55-3.2a-task-c1-local-verification.md`. Full-stack Supabase verification (PostgREST + Realtime + GoTrue) deferred until Docker is up, or folds into C3 staging shadow.
- Next active step (requires user input / ops access): **C2** iOS end-to-end smoke on a staging Supabase project with the migration applied — verify unchanged 3.1 iOS client keeps working (client-side `getAccessibleChannelMap` mirror remains as defense in depth during shadow). **C3** Staging shadow ≥24h — watch `notification-provider.tsx:81-88` global `@mention` listener + `dashboard/chat/page.tsx:200` active-channel Realtime for continued in-member delivery, `chat_channels` / `chat_messages` SELECT error rate for regressions. After C2+C3 green, decide Task D (D1 annotate iOS client gate "DB-enforced, kept as defense in depth" vs D2 aggressively collapse the client-side membership union to a no-op pass-through). Winston 3.2a re-audit triggers after A+B+C+D land.

## Blocked / Pending
- 13 modules remain on `ParityBacklogDestination`: `okr`, `deliverables`, `approval`, `request`, `leaves`, `hiring`, `team`, `announcements`, `activity`, `aiAnalysis`, `finance`, `analytics`, `admin`.
- Projects parity still NOT fully delivered:
  - **Resolution write-back** (close / reopen / add effectiveness note / add governance note): still absent on iOS. `risk_actions.update` requires role gate `['super_admin', 'admin', 'hr_admin', 'manager']` — belongs with a write-path sprint.
  - **"转为风险动作" sync from iOS** (`syncRiskFromDetection`): still deferred (carry-over from 2.4). Write path, role-gated.
  - **Governance intervention write**: still absent on iOS. Web-only.
  - **Generate risk analysis from iOS** (2.3 carry-over): still deferred. Requires Web to expose `/api/ai/project-risk` (or a Supabase Edge Function).
  - **LLM-generated narrative on AI summary** (2.2 carry-over): still deferred behind Web-side `/api/ai/project-summary`.
  - **Assignee hydrate on recent resolutions**: Web's preview doesn't surface it either; iOS matches. A later "resolution detail modal" round could hydrate.
  - **Auto-fetch on detail load**: iOS gates resolution-feedback fetch behind an explicit button tap; Web auto-fetches. Recorded as foundation divergence.
  - ~~**Governance priority when both signals fire**: iOS collapses to a single `.needsIntervention` banner (danger trumps success); Web renders both. Intentional single-banner affordance on iOS.~~ **Resolved in 2.5.1**: priority now matches Web's effective-first; prone-to-reopen renders independently.
  - **`total` caps at 50**: both iOS and Web cap at `.limit(50)` — matches Web's contract. Not a bug.
  - **Source-of-truth divergence on task count** (2.1): iOS list card shows task count, Web does not. Still open on the Web side.
  - **`risk_items` JSONB**: not parsed or displayed on iOS. Belongs with a later surface (grouping resolutions by risk item).
  - **Re-fetch-on-write coherence**: 2.5 does NOT auto-refresh resolution feedback when risk analysis / linked actions change elsewhere. User taps "Refresh". Foundation-acceptable.
  - **Status / severity / category / effectiveness vocabulary expansion**: iOS decodes as `String` + defensive UI switch with neutral fallback, so future server-side additions degrade gracefully instead of crashing decode.
  - **Palette debt**: reopened-count badge and ineffective effectiveness capsule use `Color.red.opacity(0.85)` — brand critical/danger token would replace. Shared debt with 2.3 critical risk / 2.4 high severity.
  - **RLS trust**: relies on `risk_actions` and `project_risk_summaries` RLS policies to gate cross-project reads. Same posture as every other direct Supabase read in the Projects module.
  - **Locale-aware copy**: "Check for resolution feedback" / "Checking…" / "Refresh" / "Try again" / "Resolution Feedback" / "Read-only · resolution write-back…" / "{total} tracked" / "No risk analysis exists…" / "No risk actions tracked yet…" / "Resolved/Dismissed/Active/Follow-up/Reopened" / "Intervention effective" / "Needs governance intervention" / "Prone to reopen" / "Dominant category" / "Recent resolutions" are all English-only. Belongs with broader iOS i18n.
  - Client-side `filteredProjects`, client-side `AccessOutcome` role normalization, batched `.in("id", values: ids)` hydrate instead of true nested selects, `AsyncImage` cache not persistent, date-only `String` fields, `maybeSingle()` absent from Swift SDK, task-count aggregate scaling — all unchanged carry-overs from 1.5–2.4.
- OKRs, Leaves, Deliverables still have empty `Features/` folders.
- Assignee picker remains deferred from 1.1.
- Nested `NavigationStack` inside `ProjectListView` still present (inherited from 1.3 pattern; 1.4–2.5 all confirm detail + edit sheet + delete confirmation + task-count hydrate + AI summary section + risk analysis section + linked-actions section + resolution-feedback section don't compound it).
