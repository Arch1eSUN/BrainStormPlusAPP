# 1.0-0.3 — Final Ledger Wording Fix

**执行模型：Claude Opus 4.6**

## 0. 本轮定位

`1.0-0.2` Winston 审计未通过，但失败原因只剩极少量旧句子未删干净。

必须先阅读：

- `docs/parity/12-winston-audit-1.0-0.2.md`
- `docs/parity/11-winston-audit-1.0-0.1.md`
- `docs/parity/10-winston-audit-1.0.md`

本轮是**纯 wording fix**：

- 不改 Swift 代码
- 不改 build 配置
- 不扩展 scope
- 只删/改少数残留旧句子

---

## 1. 必须修正的残留句子

### 1.1 `progress.md`

删除或改写以下过期句子：

- `Scanner returned perfectly clean.`
- `Creating docs/parity/09-winston-ready-1.0-notes.md.`
- `Proceed to Winston 1.0 Audit.`

改成当前真实状态，例如：

- ledger sync completed after Winston identified stale wording
- remaining known debt is status enum mismatch and deferred picker scope
- next step is Winston `1.0-0.3` audit

### 1.2 `findings.md`

删除或改写以下过期句子：

- `Found TaskListView.swift with line // TODO: Open New Task Sheet`
- `We deliberately omitted assignee_id/project_id picker ...`

改成当前真实状态，例如：

- create-sheet TODO has already been removed and repaired
- no assignee/project picker UI exists yet
- current implementation uses authenticated-user-derived fallback ownership fields for create flow
- picker UX remains later parity debt

---

## 2. 验证

运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n 'Scanner returned perfectly clean|Creating `docs/parity/09-winston-ready-1.0-notes.md`|Proceed to Winston 1.0 Audit|Found `TaskListView.swift` with line `// TODO: Open New Task Sheet`|We deliberately omitted `assignee_id`/`project_id` picker' progress.md findings.md || true
```

期望：**无输出**。

然后再运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n 'owner_id|reporter_id|created_by|assignee_id|progress|yyyy-MM-dd|review|in_review|canceled|1\.0-0\.3' docs/parity/09-winston-ready-1.0-notes.md progress.md findings.md task_plan.md devprompt/README.md || true
```

期望：

- 当前真实口径仍可被扫描到
- 没有回退到旧叙述

Build 不需要重跑，除非有人误改 Swift 代码。

---

## 3. 禁止事项

- 不要改 Swift 源码
- 不要新建 `1.1`
- 不要把 `1.0` 直接标成通过
- 不要删除 Winston 审计文件

---

## 4. 完成标准

1. 上述残留句子全部清除或改写。
2. 第一个 rg 验证无输出。
3. 最终回复：`建议进入 Winston 1.0-0.3 审计`。
