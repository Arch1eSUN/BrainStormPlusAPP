# 0.0-0.3 — 最终账本文案修正轮：移除 progress 中最后一个模糊验证表述

**执行模型：Gemini 3.1 Pro**

## 0. 本轮定位

这是一个**极小修复轮**。

`0.0-0.2` 已经几乎全部通过，唯一剩余阻断项是：

- `progress.md` 里仍存在模糊验证表述：`verified clean state`

本轮不做任何功能改动，不做结构改动，不做文档大改，只做：

1. 修正 `progress.md` 这句表述
2. 重新扫描
3. 重新 build
4. 回到 Winston 审计

必须先阅读：
- `docs/parity/07-winston-audit-0.0-0.2.md`

---

## 1. Skills 规则

进入本轮前，仍必须先扫描本地所有 skills，再选最匹配 skills，并强制使用。

本轮至少必须使用：

1. `planning-with-files`
2. `verification-before-completion`
3. `systematic-debugging`
4. `mgrep-code-search`
5. `receiving-code-review`

---

## 2. 唯一必须修复的内容

### 更新 `progress.md`

把类似下面这种模糊表述去掉：

```markdown
successfully verified clean state
```

改成精确表述，必须同时包含三层意思：

1. app source 中 `if true` / `PlaceholderDestination` 已清理
2. `TaskListView.swift` 的 `TODO: Open New Task Sheet` 仍存在，属于已知未修复项
3. 历史审计报告中的旧文本不应被视为当前源码缺陷

---

## 3. 禁止事项

- 不改业务代码
- 不改 `AppModule.swift`
- 不改 `ActionItemHelper.swift`
- 不新增 prompt beyond this round
- 不进入 `1.0`

---

## 4. 验证要求

### 扫描
运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n "if true|PlaceholderDestination|TODO: Open New Task Sheet|verified clean state" Brainstorm+ task_plan.md findings.md progress.md docs/parity/0[0-4]*.md
```

要求：

- `progress.md` 中不再出现 `verified clean state`
- `TaskListView.swift` TODO 仍可存在，并被准确记录

### Build
运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
xcodebuild build -project Brainstorm+.xcodeproj -scheme Brainstorm+ -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" CODE_SIGNING_ALLOWED=NO
```

---

## 5. 完成标准

1. `progress.md` 中最后一个模糊验证表述被移除。
2. 扫描通过。
3. build 通过。
4. 最终回复写：`建议再次进入 Winston 审计`。
