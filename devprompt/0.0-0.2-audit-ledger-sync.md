# 0.0-0.2 — 审计账本同步轮：收口计划/发现/文档与真实代码状态

**执行模型：Gemini 3.1 Pro**

## 0. 本轮定位

这是 `0.0-0.1` 之后的第二个修复轮。

Winston 审计结果：`0.0-0.1` **仍未通过**。

不是功能失败，也不是 build 失败，而是**账本不同步**：

- `task_plan.md` 仍未勾掉已经完成的事项
- `findings.md` 对当前 ActionItemHelper / AppModule 状态描述仍不精确
- `progress.md` 对扫描结果表述过宽
- `docs/parity/02-ios-current-state.md` 仍把已修复事项写成“正在修复中”

本轮是一个**窄范围账本同步轮**。

目标只有一个：

> 让计划、发现、进展、parity 文档，与当前真实代码状态完全一致，并再次通过扫描与 build。

本轮完成前，不允许进入 `1.0`。

必须先阅读：
- `docs/parity/06-winston-audit-0.0-0.1.md`

---

## 1. Skills 规则

每轮开始前必须先扫描本地所有 skills，再选择最匹配 skills，并强制使用。

本轮至少必须使用：

1. `planning-with-files`
2. `verification-before-completion`
3. `systematic-debugging`
4. `mgrep-code-search`
5. `lint-and-validate`
6. `receiving-code-review`
7. `find-bugs`

如果某个 skill 不可用，必须在 `progress.md` 记录替代流程。

---

## 2. 必须先阅读的文件

1. `docs/parity/06-winston-audit-0.0-0.1.md`
2. `task_plan.md`
3. `findings.md`
4. `progress.md`
5. `docs/parity/02-ios-current-state.md`
6. `docs/parity/04-ios-parity-foundation-plan.md`
7. `Brainstorm+/Shared/Navigation/AppModule.swift`
8. `Brainstorm+/Features/Dashboard/ActionItemHelper.swift`
9. `Brainstorm+/Features/Dashboard/DashboardView.swift`

---

## 3. 必须修复的问题

### 3.1 修正 `task_plan.md`

要求：

- 将以下已完成事项勾选为完成：
  - `Enhance AppModule.swift with required capabilities from Web`
  - `Shift ActionItemHelper to use strict module match instead of fuzzy text match`
- 保证 plan 与真实代码一致。

---

### 3.2 修正 `findings.md`

要求：

- 不再写“ActionItemHelper needs stricter AppModule input format”这种已经过期的表述。
- 要准确写成：
  - `destination(for module: AppModule)` 已存在
  - `destination(for title: String)` 仍保留为兼容层
  - `DashboardView` 当前 quick actions 仍通过字符串入口调用，因此真正的 caller migration 仍在后续轮次
- 保留 `TaskListView.swift` 的 TODO 为真实未完成项。

---

### 3.3 修正 `progress.md`

要求：

- 不要写模糊的“verified clean state”。
- 要准确写出：
  - app source 中 `if true` / `PlaceholderDestination` 已清理
  - `TaskListView.swift` TODO 仍存在且属已知未修复项
  - 历史审计报告中存在旧文本是允许的，不应算当前源码问题

---

### 3.4 修正 `docs/parity/02-ios-current-state.md`

要求：

- 不再写“ActionItemHelper fuzzy routing is being rectified in 0.0-0.1 phase”
- 改成当前真实状态：
  - 模块主入口 `destination(for module: AppModule)` 已存在
  - 兼容层 `destination(for title: String)` 仍存在
  - `DashboardView` 当前 quick actions 仍使用 title 调用，因此彻底 caller migration 未完成

---

### 3.5 可选修正 `docs/parity/04-ios-parity-foundation-plan.md`

如有必要，补一句：

- 模块主入口 API 已建立
- 下一阶段重点不再是“发明 API”，而是推动调用方切换到模块驱动

---

## 4. 禁止事项

- 不新增大范围功能
- 不修改 Supabase schema
- 不进入 `1.0`
- 不生成正式下一轮 prompt
- 不把历史审计报告里的旧文本当作当前源码缺陷处理

---

## 5. 验证要求

### 5.1 扫描
完成后必须运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n "if true|PlaceholderDestination|TODO: Open New Task Sheet|destination\(for module|requiredCapabilities|requiredPrimaryRoles" Brainstorm+ task_plan.md findings.md progress.md docs/parity/0[0-4]*.md
```

期望：

- `AppModule.requiredCapabilities` 存在
- `ActionItemHelper.destination(for module:)` 存在
- `TaskListView.swift` TODO 仍可存在，并被准确记录
- `task_plan.md` / `findings.md` / `progress.md` / `docs/parity/02-ios-current-state.md` 不再出现过期叙述

### 5.2 Build
必须运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
xcodebuild build -project Brainstorm+.xcodeproj -scheme Brainstorm+ -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" CODE_SIGNING_ALLOWED=NO
```

---

## 6. 完成标准

本轮完成必须同时满足：

1. `task_plan.md` 与真实代码一致。
2. `findings.md` 与真实代码一致。
3. `progress.md` 对扫描与验证结果表述精确。
4. `docs/parity/02-ios-current-state.md` 与真实代码一致。
5. 扫描通过。
6. build 通过。
7. 最终回复必须明确写：`建议再次进入 Winston 审计`。

---

## 7. 最终回复格式

只返回：

1. 同步了哪些账本文件。
2. 去掉了哪些过期叙述。
3. 当前仍存在但被明确记录的真实遗留项。
4. 扫描结果。
5. xcodebuild 结果。
6. 是否建议再次进入 Winston 审计。
