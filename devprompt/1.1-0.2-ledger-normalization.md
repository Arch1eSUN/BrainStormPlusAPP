# 1.1-0.2 — Ledger Normalization Only

**执行模型：Claude Opus 4.6**

## 0. 本轮定位

`1.1-0.1` Winston 审计未通过，但功能代码与 build 已通过。

必须先阅读：

- `docs/parity/17-winston-audit-1.1-0.1.md`
- `docs/parity/16-winston-audit-1.1.md`

本轮是**纯 ledger normalization**：

- 不改 Swift 代码
- 不重构 UI
- 不扩展 scope
- 只整理 `progress.md` 和 `task_plan.md` 的当前轮次真实性与格式

---

## 1. 修复 `progress.md`

当前问题：文件标题仍是 Sprint 1.0，并且把大量 1.0 历史修复与当前 1.1 状态混在同一个 Completed 区。

必须改成清晰结构，例如：

```md
# Progress: Sprint 1.1 (Task Status Enum + Picker Parity)

## Completed
- 1.0 Navigation & Tasks Create Flow was certified complete by `docs/parity/14-winston-audit-1.0-0.4.md`.
- 1.1 status enum parity implemented: active iOS status values now match Web TS union (`todo`, `in_progress`, `review`, `done`).
- `.canceled` removed as live status path; destructive action maps to `deleteTask`.
- Project picker UI exists and writes optional `project_id`.
- Project data now loads on initial Tasks page entry via `fetchProjects()` in `.task`.
- Refresh still reloads tasks and projects.
- Assignee picker remains deferred because people/profile/team selection data is outside current TaskListViewModel scope.
- Independent build passed for 1.1-0.1.

## Active
- Ready for Winston 1.1-0.2 audit.

## Blocked / Pending
- Assignee picker remains deferred parity debt.
- Remaining non-Tasks modules are outside this sprint scope.
```

Requirements:

- Do not keep a misleading `Sprint 1.0` title.
- Do not say `Independent build must pass` after build has passed; use completed-state wording.
- Do not re-list noisy 1.0 repair chain details inside current 1.1 progress unless clearly summarized as certified complete.

---

## 2. 修复 `task_plan.md`

Current malformed line:

```text
- - [x] 1.1-0.1 Project Picker Data Load & Ledger Sync completed.
```

Must become:

```text
- [x] 1.1-0.1 Project Picker Data Load & Ledger Sync completed.
- [x] 1.1-0.2 Ledger Normalization completed.
- Next step is Winston 1.1-0.2 audit.
```

No double bullets.

---

## 3. 验证

Run:

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n 'Sprint 1\.0|Independent build must pass|- - \[x\]|1\.1-0\.2 Ledger Normalization completed|Next step is Winston 1\.1-0\.2 audit|fetchProjects\(\)|project_id|deleteTask|review|canceled' progress.md task_plan.md
```

Expected:

- no `Sprint 1.0` title in `progress.md`
- no `Independent build must pass`
- no `- - [x]`
- must include `1.1-0.2 Ledger Normalization completed`
- must include `Next step is Winston 1.1-0.2 audit`
- current 1.1 truth remains documented

Build is not required unless Swift files are modified.

---

## 4. 禁止事项

- Do not modify Swift files.
- Do not create `1.2`.
- Do not remove Winston audit reports.
- Do not hide assignee picker deferred debt.

---

## 5. 完成标准

1. `progress.md` normalized to current 1.1 truth.
2. `task_plan.md` bullet fixed and 1.1-0.2 state added.
3. Verification scan passes.
4. Final reply: `建议进入 Winston 1.1-0.2 审计`。
