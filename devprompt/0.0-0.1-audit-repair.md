# 0.0-0.1 — 审计修复轮：修正文档与代码基础的一致性，补齐 0.0 审计缺口

**执行模型：Gemini 3.1 Pro**

## 0. 本轮定位

这是 `0.0` 的修复轮，不是 `1.0`。

Winston 审计结果：`0.0` **不通过**，原因不是 build 失败，而是：

- 文档与代码状态不一致
- 计划文件未收口
- 当前模块路由基础仍部分依赖裸字符串
- `AppModule` 元数据尚不完整

本轮目标是把 `0.0` 真正收口到可审计通过的状态。**本轮完成前，不允许生成或进入 `1.0`。**

审计报告见：
- `docs/parity/05-winston-audit-0.0.md`

你必须先完整阅读该审计报告，再开始修复。

---

## 1. Skills 规则

进入本轮前，仍然必须先扫描本地所有 skills，再选择本轮最匹配 skills，并强制使用。

本轮至少必须使用：

1. `planning-with-files`
2. `ios-development-best-practices`
3. `verification-before-completion`
4. `systematic-debugging`
5. `mgrep-code-search`
6. `supabase-postgres-best-practices`（涉及 capability / role / schema 映射时）
7. `native-data-fetching`（若触及 API bridge / session / network logic）

如果某个 skill 不可用，必须在 `progress.md` 记录替代流程。

---

## 2. 必须先阅读的文件

1. `devprompt/0.0-current-foundation-parity.md`
2. `docs/parity/05-winston-audit-0.0.md`
3. `task_plan.md`
4. `findings.md`
5. `progress.md`
6. `docs/parity/00-source-of-truth.md`
7. `docs/parity/01-web-module-map.md`
8. `docs/parity/02-ios-current-state.md`
9. `docs/parity/03-design-token-map.md`
10. `docs/parity/04-ios-parity-foundation-plan.md`
11. `Brainstorm+/Features/Dashboard/MainTabView.swift`
12. `Brainstorm+/Features/Dashboard/ActionItemHelper.swift`
13. `Brainstorm+/Shared/Navigation/AppModule.swift`
14. `Brainstorm+/Shared/Security/RBACManager.swift`
15. `../BrainStorm+-Web/src/lib/nav-config.ts`
16. `../BrainStorm+-Web/src/lib/capabilities.ts`
17. `../BrainStorm+-Web/src/lib/rbac.ts`

---

## 3. 必须修复的问题

### 3.1 更新 planning / findings / progress，保证与真实状态一致

必须修复：

- `task_plan.md`
- `findings.md`
- `progress.md`

要求：

- 不能继续保留已完成事项为未完成状态。
- 不能继续把已经修复掉的 `if true`、`PlaceholderDestination` 当作当前事实。
- `progress.md` 需要记录本轮修复内容与最终验证结果。

---

### 3.2 修复 `docs/parity/02-ios-current-state.md` 的失真问题

要求：

- 更新成与当前代码真实一致。
- 明确区分：
  - 已修复
  - 仍存在
  - 本轮未覆盖但确实存在的后续问题
- `TaskListView.swift` 的 `TODO: Open New Task Sheet` 要继续保留为未修复事实，不能假装完成。

---

### 3.3 强化 `AppModule.swift`

要求：

- 保留现有：
  - id
  - displayName
  - webRoute
  - iconName
  - group
  - implementationStatus
- 新增并补齐：
  - `requiredCapabilities: [Capability]`
  - 如确有必要再加 `requiredPrimaryRoles: [PrimaryRole]`
- 这些字段必须基于 Web 端证据，不允许编造。
- 如果某模块权限无法在 Web 证据中确定，明确标记为空并在文档说明。

---

### 3.4 收紧 `ActionItemHelper`

当前问题：仍然依赖 `destination(for title: String)` + `contains(...)` 这种模糊字符串匹配。

要求：

- 增加一个模块主入口，例如：
  - `destination(for module: AppModule)`
- `title: String` 版本如果必须保留，只能作为兼容层，不能再是核心导航基座。
- 对已实现模块继续返回真实 screen。
- 对未实现模块返回 `ParityBacklogDestination`。
- 让模块枚举真正成为导航 source of truth 的一部分。

---

### 3.5 文档同步到修复后状态

至少更新：

- `docs/parity/02-ios-current-state.md`
- `docs/parity/04-ios-parity-foundation-plan.md`

要求：

- 清楚写出：当前 `AppModule` 已经成为模块基础枚举，但核心路由仍处于基础收口阶段。
- 清楚写出：当前已移除 Copilot `if true` hack。
- 清楚写出：当前未完成事项和真实遗留项。

---

## 4. 禁止事项

- 不进入 `1.0`
- 不生成新的正式下一轮 prompt
- 不做大范围新功能开发
- 不改 Supabase schema
- 不进行无证据权限编造
- 不把文档写成“好看”但失真

---

## 5. 验证要求

### 5.1 扫描
完成后必须运行并检查：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n "if true|PlaceholderDestination|TODO: Open New Task Sheet|Coming Soon" Brainstorm+ docs/parity task_plan.md findings.md progress.md
```

要求：

- `if true` 不应再作为当前代码事实出现
- `PlaceholderDestination` 不应再被写成当前导航方案
- `TODO: Open New Task Sheet` 可以存在于源码，但必须在文档中被如实记录为未完成事实

### 5.2 Build
必须运行：

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
xcodebuild build -project Brainstorm+.xcodeproj -scheme Brainstorm+ -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" CODE_SIGNING_ALLOWED=NO
```

如该模拟器不可用，先列出可用 simulator 再替换。

---

## 6. 完成标准

本轮完成必须同时满足：

1. `docs/parity/05-winston-audit-0.0.md` 中的 blocking issues 被逐项修复。
2. `task_plan.md` 与真实完成状态一致。
3. `findings.md` 与真实代码状态一致。
4. `docs/parity/02-ios-current-state.md` 与真实代码状态一致。
5. `AppModule.swift` 增加 capability / role hooks（在有 Web 证据的前提下）。
6. `ActionItemHelper` 增加 `destination(for module: AppModule)` 这类模块主入口。
7. build 成功。
8. 不生成 `1.0`。
9. 最终回复明确写“建议再次进入 Winston 审计”。

---

## 7. 最终回复格式

只返回：

1. 修复了哪些审计问题。
2. 修改了哪些文件。
3. 哪些问题仍然存在但被明确记录。
4. 扫描结果。
5. xcodebuild 结果。
6. 是否建议再次进入 Winston 审计。
