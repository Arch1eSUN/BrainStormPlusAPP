# 3.0 — RBAC Infrastructure Web Parity Foundation

**执行模型：Claude Opus 4.6**

## 0. 轮次定位

`2.6 Projects Risk Action Sync Write Path Foundation` 已通过 Winston 独立审计（PASS，3 条非阻塞文档错误已登记）：

- `docs/parity/50-winston-audit-2.6.md`

在完成 2.6 后，我们用两个并行 subagent 对 Web / iOS 做了全量扫描，发现 Web 在 iOS 停 2.6 期间已经完成**一次关于身份模型的小重构**，iOS 的 RBAC 基础设施与 Web 产生系统性偏差。本轮目标是**把这条偏差一次性校齐**，为后续 Sprint 3.1 (Dashboard 3-template) / 3.2 (AppModule gate) 等写 UI 变更奠基。

**本轮只改 RBAC 基础设施 + 消费它的最小接口面**，不做 UI 层重组。

---

## 1. 开发前强制动作

### 1.1 重新扫描/确认 skills

至少确认并实际使用以下 skills：

- `planning-with-files`（任务清单本身是 step-by-step plan）
- `verification-before-completion`（每个 Task 结束都要 `xcodebuild build -destination "generic/platform=iOS"`）
- `systematic-debugging`（如果出现 switch exhaustiveness 错误链，按系统性步骤修而不是一把梭）
- `receiving-code-review`（自查 + Winston 审计）
- `ios-development-best-practices`（若不存在按仓内 SwiftUI / Supabase 模式替代并登记）

### 1.2 必须重新读取的 Web 源头文件

- `BrainStorm+-Web/src/lib/capabilities.ts`
  - `PrimaryRole` 类型定义（`line 10`，3 个 case）
  - `Capability` 枚举（`line 16-51`，30 个 case，5 组）
  - `DEFAULT_CAPABILITIES` 常量（`line 71-132`）
  - `getEffectiveCapabilities(primaryRole, assigned, excluded)`（`line 152-163`）
  - `CAPABILITY_PACKAGES`（`line 255-298`，本轮不复刻到 iOS，但要读懂 `hr_package` 的 10 条能力构成）
- `BrainStorm+-Web/src/lib/role-migration.ts`
  - `MIGRATION_MAP` 表（`line 36-101`）—— 11 条 legacy → canonical 映射
  - `migrateLegacyRole(oldRole)`（`line 113-139`）—— 含 deprecation warning
- `BrainStorm+-Web/src/lib/rbac.ts`
  - `UserProfile` 接口字段（`line 18-33`），特别是 `capabilities?: string[]` 与 `excluded_capabilities?: string[]`
- `BrainStorm+-Web/supabase/migrations/059_profiles_excluded_caps.sql`
  - 确认 DB 列名、类型、默认值

### 1.3 必须重新读取的 iOS 现状

- `Brainstorm+/Shared/Security/RBACManager.swift`（149 行，全量理解）
- `Brainstorm+/Core/Models/Profile.swift`（33 行，字段清单）
- `Brainstorm+/Shared/Supabase/SessionManager.swift`（当前 currentProfile cache 实现）
- `Brainstorm+/Features/Projects/ProjectListViewModel.swift:100-165`（`.chairperson` 消费点）
- `Brainstorm+/Features/Projects/ProjectDetailViewModel.swift:418` 与 `1398-1530`（`.chairperson` + `canSyncRiskAction` + `canUpdateRiskAction`）
- `Brainstorm+/Features/Projects/ProjectDetailView.swift:195-210, 970-985`（两个按钮的 gate 点）

---

## 2. 地面真相（Web vs iOS 偏差总览）

| 维度 | Web（真相） | iOS（当前） | 偏差 |
| --- | --- | --- | --- |
| `PrimaryRole` 枚举 case | 3（employee / admin / superadmin） | 4（多出 `.chairperson`） | **删** 1 个 case |
| `Capability` 枚举 case | 30 | 26 | **补** 4 条管理扩展能力 |
| `DEFAULT_CAPABILITIES[admin]` | 18 条 | 5 条 | **补 13** 条（HR 包 + 审批兜底 + 4 条管理扩展） |
| `DEFAULT_CAPABILITIES[superadmin]` | 29 条 | 25 条 | **补 4** 条（全部是管理扩展） |
| `migrateLegacyRole("chairperson")` → PrimaryRole | `'superadmin'` | `.chairperson` | **改映射**：降格到 `.superadmin` |
| `Profile.excluded_capabilities` 字段 | 存在（`string[]`） | 缺失 | **加**字段 + CodingKey |
| `getEffectiveCapabilities` 算法 | `(defaults ∪ assigned) − excluded` | `defaults ∪ migrated ∪ assigned`（**无排除**） | **补**排除逻辑 |
| legacy role 警告 | `console.warn [role-migration]` | 静默 | **加** `print` 警告（可观察迁移残余） |

**4 条新增 Capability（Web 叫"管理扩展能力"）：**

| case | Web 注释 | 所属 DB 列 / 用途 |
| --- | --- | --- |
| `attendance_admin` | 考勤规则 / 地理围栏 / 假勤豁免管理 | Admin 可配置考勤规则 |
| `leave_quota_admin` | 调休额度管理 | Admin 可调整员工调休额度 |
| `ai_evaluation_access` | AI 月度评估（跨部门） | Admin 可评估所有非超管员工，不限本部门 |
| `holiday_admin` | 公休日历管理 | Admin 可维护公休日历 |

---

## 3. 任务清单

### Task A — 扩充 Capability 枚举（+4 条管理扩展）

**文件**：`Brainstorm+/Shared/Security/RBACManager.swift`

**位置**：在 AI 能力块 `case ai_interview_entry`（现 line 36）之后、系统治理能力块 `case ai_provider_admin`（现 line 38）之前插入。

**增加内容**：

```swift
    // 管理扩展能力
    case attendance_admin       // 考勤规则 / 地理围栏 / 假勤豁免管理
    case leave_quota_admin      // 调休额度管理
    case ai_evaluation_access   // AI 月度评估 (跨部门)
    case holiday_admin          // 公休日历管理
```

**验证**：`grep -c "case " Brainstorm+/Shared/Security/RBACManager.swift` 对 Capability 枚举应得 **30**。

### Task B — 删除 `.chairperson` PrimaryRole case + 清理 6 个消费点

#### B.1 `RBACManager.swift` 内删除

1. **Line 4-9** 枚举定义：删掉 `case chairperson`（剩 3 个 case）。
2. **Line 62-67** `attendanceRules` 表：删掉 `.chairperson` 这一项（剩 3 项）。
3. **Line 69-93** `defaultCapabilities` 表：删掉 `.chairperson: [...]` 这一块（剩 3 个 key）。
4. **Line 100-101** `migrateLegacyRole` 的 `case "chairperson":` 分支先保留但改映射——见 Task D。

#### B.2 `ProjectListViewModel.swift` 修正

- **Line 165** `case .admin, .superadmin, .chairperson: return true` → `case .admin, .superadmin: return true`
- **Line 100** 文档注释 `'admin', 'superadmin', 'chairperson', 'super_admin'` → `'admin', 'superadmin'`（`chairperson` / `super_admin` 现在都已折叠到 superadmin）

#### B.3 `ProjectDetailViewModel.swift` 修正

- **Line 418** `case .admin, .superadmin, .chairperson: return true` → `case .admin, .superadmin: return true`
- **Line 1398** 注释 `chairperson → .chairperson` → 删除或改为 `chairperson → .superadmin (via migrateLegacyRole)`
- **Line 1401-1402** 注释谈 `false-positive for chairperson / team_lead` —— chairperson 现在已正式折叠到 superadmin 不是假阳性，重写这段说明。

#### B.4 `ProjectDetailView.swift` 修正

- **Line 198** 注释 `chairperson / team_lead` —— `chairperson` 部分改成 `chairperson (legacy → superadmin)`。

**验证**：
- `grep -rn "chairperson\|\.chairperson" Brainstorm+/` 应只剩 `migrateLegacyRole` 里的字符串字面量 `"chairperson"` 以及 legacy-path 注释。**不允许**再出现 `.chairperson`（Swift 枚举成员引用）。
- `xcodebuild build -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` 应 **BUILD SUCCEEDED**（去掉 case 后 switch 会变 exhaustive，不需要 `@unknown default`）。

### Task C — 重写 `defaultCapabilities` 与 Web 完全对齐

**文件**：`Brainstorm+/Shared/Security/RBACManager.swift`（当前 line 69-93）

**替换整块为以下内容**（严格对应 Web `capabilities.ts:71-132`）：

```swift
public let defaultCapabilities: [PrimaryRole: [Capability]] = [
    .employee: [],
    .admin: [
        // HR package core — admin 默认落在人事
        .hr_ops,
        .leave_approval,
        .attendance_exception_approval,
        .recruitment_approval,
        .ai_resume_screening,
        .ai_interview_entry,
        // Base approvals every admin can sign off
        .approval_access,
        .field_work_approval,
        .daily_log_approval,
        .weekly_report_approval,
        // Admin-as-fallback approver
        .reimbursement_approval,
        .purchase_approval,
        .expense_approval,
        // AI assistant
        .ai_chatbot_access,
        // 管理扩展
        .attendance_admin,
        .leave_quota_admin,
        .ai_evaluation_access,
        .holiday_admin,
    ],
    .superadmin: [
        .approval_access,
        .leave_approval,
        .attendance_exception_approval,
        .purchase_approval,
        .expense_approval,
        .reimbursement_approval,
        .recruitment_approval,
        .document_publish_approval,
        .field_work_approval,
        .daily_log_approval,
        .weekly_report_approval,
        .ai_chatbot_access,
        .ai_provider_admin,
        .apikey_admin,
        .role_policy_admin,
        .sensitive_settings_read,
        .sensitive_settings_write,
        // Functional capabilities
        .hr_ops,
        .finance_ops,
        .media_ops,
        .ai_resume_screening,
        .ai_media_analysis,
        .ai_finance_docs,
        .ai_finance_reports,
        .ai_finance_data_processing,
        .ai_interview_entry,
        // 管理扩展
        .attendance_admin,
        .leave_quota_admin,
        .ai_evaluation_access,
        .holiday_admin,
    ]
]
```

**验证**：
- `.admin` 数组 count 应为 **18**。
- `.superadmin` 数组 count 应为 **29**。
- 编写一个临时 Swift 一行验证：`assert(RBACManager.shared.defaultCapabilities[.admin]?.count == 18)`；`assert(RBACManager.shared.defaultCapabilities[.superadmin]?.count == 29)`；跑完即删。

### Task D — 对齐 `migrateLegacyRole` 映射表

**文件**：`Brainstorm+/Shared/Security/RBACManager.swift`（当前 line 96-123）

**替换整个方法体为**：

```swift
public func migrateLegacyRole(_ oldRoleStr: String?) -> MigrationResult {
    let canonical: Set<String> = ["superadmin", "admin", "employee"]
    let role = oldRoleStr?.lowercased() ?? "employee"

    // Canonical pass-through
    switch role {
    case "superadmin":
        return MigrationResult(primaryRole: .superadmin, capabilities: [], attendanceRule: attendanceRules[.superadmin]!)
    case "admin":
        return MigrationResult(primaryRole: .admin, capabilities: [], attendanceRule: attendanceRules[.admin]!)
    case "employee":
        return MigrationResult(primaryRole: .employee, capabilities: [], attendanceRule: attendanceRules[.employee]!)
    // Legacy → canonical (migration 049 fallback)
    case "chairperson", "super_admin":
        logLegacyRoleWarning(role, mappedTo: "superadmin")
        return MigrationResult(primaryRole: .superadmin, capabilities: [], attendanceRule: attendanceRules[.superadmin]!)
    case "manager", "team_lead":
        logLegacyRoleWarning(role, mappedTo: "admin")
        return MigrationResult(primaryRole: .admin, capabilities: [], attendanceRule: attendanceRules[.admin]!)
    case "hr":
        logLegacyRoleWarning(role, mappedTo: "employee + hr capabilities")
        return MigrationResult(
            primaryRole: .employee,
            capabilities: [.hr_ops, .approval_access, .leave_approval, .recruitment_approval, .ai_resume_screening, .ai_interview_entry],
            attendanceRule: attendanceRules[.employee]!
        )
    case "finance":
        logLegacyRoleWarning(role, mappedTo: "employee + finance capabilities")
        return MigrationResult(
            primaryRole: .employee,
            capabilities: [.finance_ops, .approval_access, .purchase_approval, .expense_approval, .reimbursement_approval, .ai_finance_docs, .ai_finance_reports, .ai_finance_data_processing],
            attendanceRule: attendanceRules[.employee]!
        )
    case "contractor", "intern":
        logLegacyRoleWarning(role, mappedTo: "employee")
        return MigrationResult(primaryRole: .employee, capabilities: [], attendanceRule: attendanceRules[.employee]!)
    default:
        // Unknown / corrupt role — deprecate to employee
        if !canonical.contains(role) {
            print("⚠️ [role-migration] unknown role \"\(role)\" → defaulting to employee. Possible corrupt profile row.")
        }
        return MigrationResult(primaryRole: .employee, capabilities: [], attendanceRule: attendanceRules[.employee]!)
    }
}

private func logLegacyRoleWarning(_ oldRole: String, mappedTo newRoleDescription: String) {
    print("⚠️ [role-migration] deprecated role \"\(oldRole)\" → \"\(newRoleDescription)\" (fallback). Migration 049 should have removed this; inspect the profile row.")
}
```

**关键差异说明**：
- `chairperson` 现在映射到 `.superadmin`（不再是已删除的 `.chairperson`）。
- 所有 legacy role 触发 `print` 警告，对应 Web 的 `console.warn [role-migration]` 钩子，便于将来观察 DB 里还有没有残余旧角色字符串。
- canonical 3 role 保持静默直通。

**验证**：
- 代码里搜 `\.chairperson` 应**零匹配**（Task B 已处理 enum 使用；这里处理字符串分支）。
- 手动 dryrun：`migrateLegacyRole("chairperson").primaryRole` 应返回 `.superadmin`；`migrateLegacyRole("hr").capabilities.count` 应返回 **6**。

### Task E — `Profile` 模型加 `excludedCapabilities` 字段

**文件**：`Brainstorm+/Core/Models/Profile.swift`

**修改**：

```swift
public struct Profile: Codable, Identifiable, Hashable {
    // ... 现有字段不动 ...
    public let capabilities: [String]?
    public let excludedCapabilities: [String]?   // ← 新增（在 capabilities 之后）
    public let createdAt: Date?
    public let updatedAt: Date?

    enum CodingKeys: String, CodingKey {
        // ... 现有映射不动 ...
        case capabilities
        case excludedCapabilities = "excluded_capabilities"   // ← 新增
        case createdAt = "created_at"
        case updatedAt = "updated_at"
    }
}
```

**验证**：
- `xcodebuild` 成功。
- 手写一个临时 JSON 解码测试：
  ```swift
  let sample = """
  {"id":"...","role":"admin","capabilities":["hr_ops"],"excluded_capabilities":["ai_chatbot_access"]}
  """.data(using: .utf8)!
  let p = try JSONDecoder().decode(Profile.self, from: sample)
  assert(p.excludedCapabilities == ["ai_chatbot_access"])
  ```
  跑完即删。

### Task F — `getEffectiveCapabilities` 消费 excluded 集合

**文件**：`Brainstorm+/Shared/Security/RBACManager.swift`（当前 line 126-144）

**替换整块为**：

```swift
public func getEffectiveCapabilities(for profile: Profile?) -> [Capability] {
    guard let profile = profile else { return [] }

    let migration = migrateLegacyRole(profile.role)
    let defaults = defaultCapabilities[migration.primaryRole] ?? []

    // Explicit capabilities written to the profile row
    let dbCapabilities: [Capability] = (profile.capabilities ?? []).compactMap { Capability(rawValue: $0) }

    // Merge defaults ∪ migration-derived ∪ DB-explicit
    var merged = Set<Capability>(defaults)
    migration.capabilities.forEach { merged.insert($0) }
    dbCapabilities.forEach { merged.insert($0) }

    // Subtract excluded_capabilities (admin can strip specific caps from a user)
    let excluded: [Capability] = (profile.excludedCapabilities ?? []).compactMap { Capability(rawValue: $0) }
    excluded.forEach { merged.remove($0) }

    return Array(merged)
}
```

**验证**（dryrun 片段）：

```swift
let profile = Profile(
    id: UUID(), role: "admin",
    capabilities: nil,
    excludedCapabilities: ["holiday_admin"]  // 剥去 admin 默认带的公休日历管理
)
let caps = RBACManager.shared.getEffectiveCapabilities(for: profile)
assert(!caps.contains(.holiday_admin))   // 应被排除
assert(caps.contains(.hr_ops))           // 其他仍在
```

### Task G — RBAC 助手统一：`canManageRiskActions(profile:)`

**动机**：2.6 审计 §8 标记的非阻塞 debt —— `ProjectDetailViewModel.canSyncRiskAction(role:)` 与 `canUpdateRiskAction(role:)` 都是临时 role 字符串白名单（`{super_admin, admin, hr_admin, manager}`），**不走** RBACManager，与 iOS RBAC 架构割裂。本轮吸收进 RBACManager 统一出口。

**步骤**：

1. **`RBACManager.swift` 新增公开方法**（挨着 `hasCapability`，line 146 之后）：

    ```swift
    /// Matches DB RLS policy for risk_actions table (migrations 014 + 037):
    /// super_admin / admin / hr_admin / manager can write. This is intentionally
    /// narrower than Web's server guard (which also allows team_lead / superadmin
    /// / chairperson) — client-side gate mirrors the authoritative DB layer.
    public func canManageRiskActions(profile: Profile?) -> Bool {
        guard let role = profile?.role?.lowercased() else { return false }
        return ["super_admin", "superadmin", "admin", "hr_admin", "manager"].contains(role)
    }
    ```

    > **注**：保留 raw role 字符串比对而非 `PrimaryRole`，因为 DB RLS 仍按 role 字符串 enforce（超过 `PrimaryRole` 分辨率）。加 `superadmin` canonical 别名以覆盖 Phase 2 下的 DB 值。

2. **`ProjectDetailViewModel.swift`**：
    - 删除 line 1408-1416 附近的 `canSyncRiskAction(role:)` 与相邻的 `canUpdateRiskAction(role:)` 本地实现。
    - 所有调用点改为 `RBACManager.shared.canManageRiskActions(profile: SessionManager.shared.currentProfile)`。
    - 调用点：line 1520 附近的 guard、ProjectDetailView.swift:195, 204-205, 970, 978, 981。

3. **`ProjectDetailView.swift`**：
    - `private var canSyncRiskAction: Bool { ... }` 改成 `private var canSync: Bool { RBACManager.shared.canManageRiskActions(profile: session.currentProfile) }`（`session` 是 `@Environment(SessionManager.self)` 或现有注入方式，视 View 签名而定）。
    - Button disabled / text gate 都改用 `canSync`。

**验证**：
- `grep -n "canSyncRiskAction\|canUpdateRiskAction" Brainstorm+/` 应只保留 `RBACManager.canManageRiskActions` 及其 docstring。
- 登录为 admin 账号（或 role mock）本地跑，"转为风险动作"按钮按原逻辑亮/暗。登录为 employee 账号应暗。
- 2.6 已有 Supabase RLS INSERT 验证路径，本次只替换 client gate 名称，不触动 write path。

### Task H — 修正 `49-winston-ready-2.6-notes.md` 文档错误

**文件**：`docs/parity/49-winston-ready-2.6-notes.md`

**修正点**（来自 `50-winston-audit-2.6.md` §8）：

1. **路径错误**：所有 `src/lib/security/rbac.ts` 改为 `src/lib/rbac.ts`。
2. **RBAC 解析错误**：ready-notes 原文声称 `serverGuard({requiredRole:'manager'})` 解析为 `{super_admin, admin, hr_admin, manager}`。改为：Web `serverGuard` 实际上等价 `{manager, team_lead, admin, super_admin, superadmin, chairperson}`（6 个 role）；iOS 白名单 `{super_admin, admin, hr_admin, manager}` 实际是 **DB RLS policy** 的镜像（迁移 014 + 037），这是安全等价但**严格更窄**的选择——写出这条澄清。

### Task I — 更新 ledger 三件套

**`findings.md`**：
- 删除 "## 2.6 Debt Carry-Forward" 里关于文档错误和 RBAC 统一的两条（它们已在 3.0 关闭）。
- 顶部 "## Active Debt" 追加（如有）3.0 扫描中新发现的问题。

**`progress.md`**：
- 替换 "## Active" 块为 `## Sprint 3.0 RBAC Infrastructure — <DATE>`，含：
  - Verdict 待 Winston 审计。
  - 本轮 9 个 Task 的完成状态。
  - BUILD SUCCEEDED 证据。
  - Recommended next step：Sprint 3.2 AppModule gate alignment（用户钦定路径 3.0 → 3.2 → 3.1）。

**`task_plan.md`**：
- 把"open 2.7"那条换成 "open 3.0 post-audit follow-up (3.2)"。

---

## 4. 验证清单（全部通过才算 DONE）

- [ ] Capability 枚举 count = **30**（`grep -c "^    case " Brainstorm+/Shared/Security/RBACManager.swift` 在 enum 块范围内）。
- [ ] PrimaryRole 枚举 count = **3**（无 `.chairperson`）。
- [ ] `grep -rn "\.chairperson" Brainstorm+/Features/` 应**零匹配**。
- [ ] `xcodebuild build -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO` **BUILD SUCCEEDED**。
- [ ] `defaultCapabilities[.admin].count == 18`、`defaultCapabilities[.superadmin].count == 29`（一次性 assert 验证后删）。
- [ ] `Profile` 解码一条含 `excluded_capabilities` 的 JSON，字段值正确。
- [ ] `getEffectiveCapabilities` 对一个手造 profile（`capabilities: nil`, `excluded: [holiday_admin]`）去掉了 `.holiday_admin`。
- [ ] `migrateLegacyRole("chairperson").primaryRole == .superadmin`。
- [ ] `canManageRiskActions(profile:)` 被 Projects 模块里 6 个调用点全部消费；老的 `canSyncRiskAction` / `canUpdateRiskAction` 已删除。
- [ ] `49-winston-ready-2.6-notes.md` 两条错误已改。
- [ ] Ledger 三件套已更新。
- [ ] 起 `docs/parity/51-winston-ready-3.0-notes.md` 草稿（下一轮 Winston 准备阅读材料——列出改动文件、关键 diff 摘要、验证结果）。

---

## 5. 风险 & 边界

### 5.1 Switch exhaustiveness 连锁反应

删除 `.chairperson` 后所有对 `PrimaryRole` 做完整 switch 的地方都会报错。已知有 2 处（ProjectListViewModel:165 / ProjectDetailViewModel:418）。全局再搜一次 `switch .*primaryRole\|switch .*PrimaryRole` 避免漏网。

### 5.2 DB 里仍可能有 `role = 'chairperson'`

migration 049 应已折叠，但部分老 profile 行可能未跑完脚本。`migrateLegacyRole` 仍保留该字符串分支并发 warn，不会 crash。**不做** DB 层修复（超出本轮范围）。

### 5.3 `excluded_capabilities` DB 列存在性

Web migration 059 已添加。若 iOS 本地 Supabase snapshot 未同步，Profile 解码 `excluded_capabilities` 会得到 `nil`——代码已用 `?? []` 容忍这种情况，无需强制 DB 同步。

### 5.4 Capability Packages 不在本轮

Web `CAPABILITY_PACKAGES`（`hr_package` / `finance_package` / `media_package`）**不**在 3.0 范围。iOS 今天用户画像都是手搓 `capabilities` 字段 + role 默认。Packages 等到 3.5+ 的 admin 设置 UI 再引入，届时做到"运行时组装 packageIds → caps 数组"。

### 5.5 Sprint 3.0 的 RBAC 变更可能影响后续 3.1 / 3.2

这是**预期中**的。3.1 Dashboard 3-template 与 3.2 AppModule gate 都会直接消费新的 `DEFAULT_CAPABILITIES` 表与 `getEffectiveCapabilities`。本轮先把地基浇稳，后续 UI 重组就是 pure view-layer 工作。

---

## 6. 范围外（明确声明）

- ❌ Dashboard 3-template 切分（留给 3.1）。
- ❌ `AppModule.requiredCapabilities` 改 any-of 语义（留给 3.2）。
- ❌ OKR / Leaves / Deliverables 空目录填充（留给 3.3 / 3.4 / 3.5）。
- ❌ `CAPABILITY_PACKAGES` 在 iOS 的对应实现（留给 3.5+）。
- ❌ Dual-province 架构（**封存**中，等用户通知再动）。
- ❌ `AttendanceRule.reportingRequired == false` 的出口处理——以前 `.chairperson` 唯一拥有的特殊规则，现 Web 三个 role 都是 `true`，本轮删除之后不再补 exception（Web 本来就没给 chairperson 特权，是 iOS 遗留）。

---

## 7. Winston Ready-Notes 产出

在所有 Task 完成且自验证通过后，**不要**直接 commit。先产出 `docs/parity/51-winston-ready-3.0-notes.md`，内容结构参照 `49-winston-ready-2.6-notes.md`：

- § 本轮 Web 真相源头（3 个关键文件 + 关键行号）
- § iOS 改动全清单（表格：文件 → 类型 → 关键行）
- § 与 Web 对照的字段级 parity 表（PrimaryRole / Capability count / DEFAULT_CAPABILITIES / getEffectiveCapabilities 算法）
- § 已知不对齐的遗留点（DB 老 role 行 / Capability Packages 缺失）
- § 验证证据（BUILD SUCCEEDED log / count 断言 output / migrateLegacyRole dryrun）

然后**才** commit + 交由 Winston 独立审计。
