# 1.0-0.2 — Ledger Sync Only for Task Create Repair

**执行模型：Claude Opus 4.6**

## 0. 本轮定位

`1.0-0.1` Winston 审计未通过，但不是代码 build 问题。

必须先阅读：

- `docs/parity/11-winston-audit-1.0-0.1.md`
- `docs/parity/10-winston-audit-1.0.md`
- `devprompt/1.0-0.1-task-create-repair.md`

本轮是极小账本修复轮：**只同步文档/ledger，不改功能代码，除非发现文档同步无法解释的真实代码矛盾。**

---

## 1. 当前真实代码状态

Winston 独立审计已确认：

1. `TaskListView.swift` 已挂载：

```swift
.sheet(isPresented: $isShowingCreateTask) {
    CreateTaskView(viewModel: viewModel)
}
```

2. `TaskListViewModel.TaskInsert` 已包含：

```swift
owner_id
reporter_id
created_by
assignee_id
progress
```

3. 这些字段由 authenticated Supabase user id 填充。

4. `due_date` 已改为：

```swift
yyyy-MM-dd
```

5. 独立 build 已通过：

```text
** BUILD SUCCEEDED **
```

---

## 2. 必须修正的文件

### 2.1 `docs/parity/09-winston-ready-1.0-notes.md`

必须删除/改正旧说法：

- “intentionally omit owner_id/reporter_id”
- “avoiding manual sysadmin UUID injections” 这类把 authenticated user id 与伪造 sysadmin 混在一起的误导表述
- “exact schema parity” 或等价过度表述

必须改为真实口径：

- iOS create payload now explicitly sends authenticated-user-derived `owner_id`, `assignee_id`, `reporter_id`, `created_by`, and `progress: 0` to match Web/RLS-critical behavior.
- No hardcoded UUIDs, no service-role bypass.
- `due_date` is encoded as `yyyy-MM-dd` for Postgres `DATE` semantics.
- Status enum still has known Web/iOS mismatch: Web uses `review`; iOS model includes `in_review` and `canceled`. Create path writes `todo`, so this does not block create, but it remains task parity debt.

### 2.2 `progress.md`

Must record `1.0-0.1`/`1.0-0.2` real state:

- sheet mounted
- RLS-critical insert fields added
- due_date format fixed
- build passed
- ledger stale issue identified by Winston and fixed in this round
- status enum debt remains

Do not leave statements claiming payload only includes title/description/priority/status/due_date.

### 2.3 `findings.md`

Must replace stale RLS reasoning.

Correct finding:

- Web create action sends ownership/reporter/creator fields.
- `012_fix_tasks_rls.sql` makes `reporter_id` / `created_by` significant for policy/visibility semantics.
- iOS create now uses current authenticated user id for owner/reporter/creator/assignee fallback.
- This is not fabricated data.
- Remaining debt: real assignee/project picker not implemented; current fallback is creator-owned task creation only.

### 2.4 `task_plan.md`

Must add/mark repair closure state:

- `1.0-0.1` code repair completed
- `1.0-0.2` ledger sync completed
- next step is Winston `1.0-0.2` audit, not `1.1`

### 2.5 `devprompt/README.md`

Set current round to:

```text
1.0-0.2-ledger-sync.md
```

---

## 3. Verification

Run:

```bash
cd /Users/archiesun/Desktop/Work/BrainStorm+/BrainStorm+-App
rg -n "intentionally omit|sysadmin|exactly with the Web|exact schema|owner_id|reporter_id|created_by|assignee_id|progress|yyyy-MM-dd|review|in_review|canceled|1\.0-0\.2" docs/parity/09-winston-ready-1.0-notes.md progress.md findings.md task_plan.md devprompt/README.md
```

Expected:

- stale “intentionally omit” no longer appears
- stale “sysadmin” justification no longer appears unless explicitly saying no hardcoded sysadmin UUIDs are used
- ownership/reporter/creator fields are documented accurately
- `yyyy-MM-dd` documented
- enum mismatch documented
- current round is `1.0-0.2-ledger-sync.md`

Build is not required if only ledger files are touched. If code changes accidentally occur, rerun full xcodebuild.

---

## 4. 禁止事项

- Do not modify Swift code unless a real contradiction is discovered.
- Do not create `1.1`.
- Do not mark `1.0` as passed.
- Do not delete Winston audit reports.
- Do not hide the enum mismatch.

---

## 5. 完成标准

1. All stale ledger statements fixed.
2. Verification rg passes.
3. Final reply: `建议进入 Winston 1.0-0.2 审计`.
