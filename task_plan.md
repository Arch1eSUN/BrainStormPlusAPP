# Brainstorm+ iOS Project Plan

## Phase 1.0: Sprint 1.0 Navigation & Tasks Create Flow
- [x] Analyze Web source of truth for Tasks (`tasks` schema, keys, types).
- [x] Typed Dashboard Routing:
  - Migrate DashboardView quick actions to strictly use `ActionItemHelper.destination(for module:)`.
  - Mark `destination(for title: String)` as deprecated in ActionItemHelper.
- [x] Tasks New Task Create Flow:
  - Find and address the `TODO: Open New Task Sheet` in `TaskListView.swift`.
  - Design a functional Create Task UI Sheet covering required variables (title, optional description, priority, due date).
  - Update `TaskListViewModel` to execute insert into Supabase `tasks` table with Encodable object conforming to database schema.

- [x] 1.0-0.1 code repair completed.
- [x] 1.0-0.2 ledger sync completed.
- [x] 1.0-0.3 final ledger wording fix completed.
- [x] 1.0-0.4 task plan next-step sync completed.
- [x] 1.1 Enum Parity and Picker Parity completed.
- [x] 1.1-0.1 Project Picker Data Load & Ledger Sync completed.
- [x] 1.1-0.2 Ledger Normalization completed.
- [x] 1.2 Dashboard Destination Parity completed.
- [x] 1.3 Projects List Foundation completed.
- [x] Winston 1.3 audit completed — `docs/parity/22-winston-audit-1.3.md` PASS.
- [x] 1.4 Projects Filter + Detail Foundation completed.
- [x] Winston 1.4 audit completed — `docs/parity/24-winston-audit-1.4.md` PASS.
- [x] 1.5 Projects Server-Side Filter + Membership Scope Foundation completed.
- [x] Winston 1.5 audit completed — `docs/parity/26-winston-audit-1.5.md` PASS.
- [x] 1.6 Projects Detail Membership Gate + Ordering Alignment Foundation completed.
- [x] Winston 1.6 audit completed — `docs/parity/28-winston-audit-1.6.md` PASS.
- [x] 1.7 Projects Owner Profile Join + Detail Enrichment Foundation prompt created.
- [x] 1.7 Projects Owner Profile Join + Detail Enrichment Foundation completed.
- [x] Winston 1.7 audit completed — `docs/parity/30-winston-audit-1.7.md` PASS.
- [x] 1.8 Projects Nested Profile Join + Avatar Rendering Foundation prompt created.
- [x] 1.8 Projects Nested Profile Join + Avatar Rendering Foundation completed.
- [x] Winston 1.8 audit completed — `docs/parity/32-winston-audit-1.8.md` PASS.
- [x] 1.9 Projects Edit + Member Management Foundation prompt created.
- [x] 1.9 Projects Edit + Member Management Foundation completed.
- [x] Winston 1.9 audit completed — `docs/parity/34-winston-audit-1.9.md` PASS.
- [x] 2.0 Projects Delete Foundation prompt created.
- [x] 2.0 Projects Delete Foundation completed.
- [x] Winston 2.0 audit completed — `docs/parity/36-winston-audit-2.0.md` PASS.
- [x] 2.1 Projects Task Count List Parity prompt created.
- [x] 2.1 Projects Task Count List Parity completed.
- [x] Winston 2.1 audit completed — `docs/parity/38-winston-audit-2.1.md` PASS.
- [x] 2.2 Projects AI Summary Foundation prompt created.
- [x] 2.2 Projects AI Summary Foundation completed.
- [x] Winston 2.2 audit completed — `docs/parity/40-winston-audit-2.2.md` PASS.
- [x] 2.3 Projects Risk Analysis Foundation prompt created.
- [x] 2.3 Projects Risk Analysis Foundation completed.
- [x] Winston 2.3 audit completed — `docs/parity/42-winston-audit-2.3.md` PASS.
- [x] 2.4 Projects Linked Risk Actions Foundation prompt created.
- [x] 2.4 Projects Linked Risk Actions Foundation completed.
- [x] Winston 2.4 audit completed — `docs/parity/44-winston-audit-2.4.md` PASS.
- [x] 2.5 Projects Resolution Feedback Foundation prompt created.
- [x] 2.5 Projects Resolution Feedback Foundation completed.
- [x] Winston 2.5 audit completed — `docs/parity/46-winston-audit-2.5.md` FAIL.
- [x] 2.5.1 minimum fix prompt created — `devprompt/2.5.1-resolution-feedback-governance-priority-fix.md`.
- [x] 2.5.1 Resolution Feedback Governance Priority Fix completed.
- [x] Winston 2.5.1 audit completed — `docs/parity/48-winston-audit-2.5.1.md` PASS.
- [x] 2.6 Projects Risk Action Sync Write Path Foundation prompt created.
- [x] 2.6 Projects Risk Action Sync Write Path Foundation completed.
- [x] Winston 2.6 audit completed — `docs/parity/50-winston-audit-2.6.md` PASS (with 3 non-blocking doc corrections folded into next sprint).
- [x] 3.0 RBAC Infrastructure Web-Parity Foundation prompt created — `devprompt/3.0-rbac-infrastructure-web-parity-foundation.md`.
- [x] 3.0 RBAC Infrastructure Web-Parity Foundation completed: dropped `.chairperson` `PrimaryRole` case (folded to `.superadmin` via `migrateLegacyRole`) + added 4 `Capability` cases (`attendance_admin` / `leave_quota_admin` / `ai_evaluation_access` / `holiday_admin`) + rebuilt `defaultCapabilities` to match Web `src/lib/capabilities.ts` (admin=18, superadmin=30; devprompt's "superadmin=29" was stale vs. Web source of truth) + added `logLegacyRoleWarning` helper on `migrateLegacyRole` for migration-049 drift detection + `getEffectiveCapabilities` now subtracts `profile.excludedCapabilities` per Web spec + added `canManageRiskActions(rawRole:)` on `RBACManager` mirroring DB RLS (`risk_actions` migrations 014 + 037) with `profile:` convenience overload. Profile model gained `excludedCapabilities: [String]?` (JSON key `excluded_capabilities`). Projects consumer cleanup: `ProjectListViewModel.isAdmin` + `ProjectDetailViewModel.isAdmin` switches drop `.chairperson`, `ProjectDetailViewModel` deletes local `canSyncRiskAction(role:)` + `syncEnabledRoles` (now routes through `RBACManager.shared.canManageRiskActions(rawRole:)`), `ProjectDetailView.canSyncRiskAction` computed property delegates to the shared manager. **Verification**: `xcodebuild build` green + 7/7 `RBAC_Sprint30_Tests` pass (defaultCapabilities counts / chairperson→superadmin fold / hr legacy → employee+derived-caps / excluded_capabilities JSON decode / `getEffectiveCapabilities` excluded subtraction / `canManageRiskActions` role whitelist). `docs/parity/49-winston-ready-2.6-notes.md` §2 RBAC passage re-grounded against re-read Web sources. Sprint 3.0 completes the auth/RBAC foundation (parity Task #12) that unblocks 3.1 Chat + 3.2 Dashboard templates + 3.3 Attendance + 3.4 Reporting + 3.5 Copilot.
- [x] 3.1 Team Chat Foundation prompt created — `devprompt/3.1-team-chat-foundation.md` (commit `fdaf782`).
- [x] 3.1 Team Chat Foundation completed: first usable Team Chat surface on iOS — channel list with Web-parity access filter, message read (`created_at ASC limit 50`), message send (insert + optimistic append + best-effort `chat_channels` tail update), Supabase Realtime v2 `INSERT` push. Added `ChatChannelMember` struct + `MemberRole` enum to `Core/Models/ChatModel.swift`. Rewrote `ChatListViewModel` to replicate Web `getAccessibleChannelMap` (chat.ts:252-282) via three parallel `async let` queries (announcement / owned / membership-resolved) + Set-based dedupe + `lastMessageAt DESC nulls last` sort. New `ChatRoomViewModel` (`@MainActor ObservableObject`, 5 methods: `bootstrap` / `fetchMessages` / `sendMessage` / `subscribeRealtime` / `teardown`) replicates Web `ensureChannelAccess` (chat.ts:284-305) before any fetch/subscribe; send flow dedups Realtime echo by id; UPDATE via migration 026 `auth.uid() IS NOT NULL` policy; realtime via SDK v2 `postgresChange(InsertAction.self, filter: .eq(...))` + `JSONObject.decode(as:)` inheriting `JSONDecoder.supabase()`. `deinit` only cancels the task (MainActor-isolated `teardown` not callable from deinit); `ChatRoomView` calls `teardown()` from `.onDisappear`. Rewrote `ChatRoomView` to bind via `@StateObject(wrappedValue:)` with accessDenied / loading-empty / normal tri-state UI + ScrollViewReader auto-scroll on `messages.count` change. New `ChatDateFormatter` (nil / today / 昨天 / 本年 M月d日 / 跨年 yyyy年M月d日) with static-cached `zh_CN` `DateFormatter` instances. Patched `ChatListView` NavigationLink to inject `ChatRoomViewModel(client: supabase, channel: channel)` and replaced `Text(date, style: .time)` with `Text(ChatDateFormatter.format(channel.lastMessageAt))`. No new FAB / search / unread-badge — all deferred to 3.1-debt-04. Attachments / reactions / withdraw / reply threading / mentions / AI Copilot explicitly out of scope per devprompt §6. **Verification**: `xcodebuild build -destination "generic/platform=iOS Simulator"` Debug with code-signing disabled → `** BUILD SUCCEEDED **`. Commits on `main`: `0e6e456` (Task A) + `f591231` (Tasks B + C) + `7b0b135` (Tasks D + E; subagent committed first — chronological landing A → B/C → D). **5 debt items carry forward** (details in `docs/parity/52-winston-ready-3.1-notes.md`, authored under Task G): 3.1-debt-01 Attachments / images / files (3.2 Attachments sprint); 3.1-debt-02 Message withdraw UI + RLS UPDATE test; 3.1-debt-03 Reply threading (render consumer of `reply_to` + Web's reply-lookup second query); 3.1-debt-04 Mentions / search / create-conversation / find-or-create-DM; 3.1-debt-05 Web RLS tightening on `chat_channels` / `chat_messages` SELECT (architectural — cross-end security alignment, not single-sprint).
- [x] 3.1 closeout pass completed: Winston audit returned PASS-with-conditions citing 3 ledger failures + unclosed error-UI + unresolved SELECT RLS. Closeout resolved (1) ledger corrections in `docs/parity/52-winston-ready-3.1-notes.md` (stale mock-Supabase cross-ref, stale subscribe/teardown descriptions) and (2) error UI via new `Shared/DesignSystem/Modifiers/ZYErrorBannerModifier.swift` wired into both `ChatRoomView` and `ChatListView` (`.zyErrorBanner($viewModel.errorMessage)`) — debt-06 + debt-07 both ✅ CLOSED in this pass. `xcodebuild` green. (3) Debt-05 was not solved in-session (SELECT RLS tightening crosses the iOS/Web boundary; not unilateral) — instead converted from vague "architectural debt" into concrete spec `devprompt/3.2a-chat-rls-tightening.md` covering migration to membership-scoped predicates, Web server-action regression scan, staging shadow verification, and optional iOS client-side simplification. Debt-09 (mock-Supabase testability harness) split out of debt-05 in the same pass to stop coupling "add tests" to "tighten RLS." Final open-debt count: 7 (01–05 original + 08 + 09; 06 + 07 closed).
- [x] 3.2 Chat Create-Conversation + Find-or-Create DM (part of 3.1-debt-04) completed — autonomous execution under "你自己决定" + "先做 1:1 移植 + app 前端，最后统一测试" cadence directives.
  - Web: migration `BrainStorm+-Web/supabase/migrations/20260421130000_chat_conversation_rpc.sql` adds two SECURITY DEFINER RPCs: `chat_find_or_create_direct_channel(p_other_user_id UUID) RETURNS UUID` and `chat_create_group_channel(p_name TEXT, p_description TEXT, p_member_ids UUID[]) RETURNS UUID`. Both `SET search_path = public`, `REVOKE FROM PUBLIC` + `GRANT EXECUTE TO authenticated`. Direct RPC: sorted-pair key (`${low},${high}` ascii-lex of uuid::text) → fast-path SELECT on partial unique index `idx_channels_direct_pair_unique` → INSERT wrapped in `BEGIN/EXCEPTION WHEN unique_violation/SELECT` race-recovery → two-row `chat_channel_members` upsert (owner self + member other). Group RPC: `NULLIF(btrim(COALESCE(...)))` name/description normalization, `unnest + WHERE m <> v_user_id + array_agg(DISTINCT)` dedup + self-strip, zero-member rejection, insert channel + owner + members with `ON CONFLICT DO NOTHING`. Validation errors raise with SQLSTATE `22023` (`invalid_parameter_value`) for bad-input and `42501` (`insufficient_privilege`) for unauth. Selected RPC-over-API-route architecture: single DB-side source of truth (Web server actions `createConversation` / `findOrCreateDirectMessage` at `src/lib/actions/chat.ts:411,758` CAN migrate to call these later; 3.2 only ADDS the functions, doesn't modify existing Web call sites). Rationale: Web currently bypasses `chat_channels` INSERT RLS (admin-gated 023:55-62) + `chat_channel_members` INSERT RLS (self-only 028:29-33) via `createAdminClient()`; iOS user-JWT can't do the same. Either route plumbing or SECURITY DEFINER — RPC wins on race-safety (can't lose the unique-violation retry in an HTTP request boundary) + future Web reuse potential.
  - Verification: 9-assertion native-Postgres harness (`/tmp/chat_rpc_test.sh`) with Supabase `authenticated` role stubbed via `CREATE ROLE` DO-block + `auth.uid()` stubbed via `current_setting('request.jwt.claim.sub', true)::uuid`. All green: (1) first-call creates direct channel with 2 member rows + counterparty-name, (2) second call fast-path returns same id + 1 direct channel total, (3) symmetric call from counterparty returns same id, (4) self-DM rejected (`invalid_parameter_value`), (5) unauth rejected (`insufficient_privilege`), (6) group happy path (3 members, 1 owner, name + description), (7) empty-name rejected, (8) zero-member + self-only-member rejected, (9) duplicate member ids deduped to 3 rows. Initial test-run surfaced and fixed missing `authenticated` role precondition.
  - iOS: `BrainStorm+-App/Brainstorm+/Features/Chat/ChatListViewModel.swift` gains 5 new methods — `findOrCreateDirectChannel(with: UUID) async throws -> UUID` (RPC call), `createGroupChannel(name:description:memberIds:) async throws -> UUID` (RPC call), `fetchChannel(id:) async throws -> ChatChannel` (post-create hydration), `appendChannelIfMissing(_:)` (top-insert if not present), `fetchUsers(search:) async throws -> [Profile]` (mirrors Web `fetchChatUsers` at chat.ts:307-353: `.neq("id", ...)` self-exclude + optional `.or("full_name.ilike.%q%,display_name.ilike.%q%")` + `.order("full_name").limit(50)`). New files: `UserPickerView.swift` (multi-select picker with 300ms debounced search + SF-symbol-initial avatars + name/department rows + `@Binding var selectedUserIds: Set<UUID>`) and `NewConversationSheet.swift` (wraps picker, conditional group-name+description fields when `selectedUserIds.count >= 2`, submit button branches `1 → findOrCreateDirectChannel else createGroupChannel` → `fetchChannel` + `appendChannelIfMissing` + dismiss). `ChatListView.swift` patched with `@State private var showNewConversation` + `.toolbar` `+` button + `.sheet(isPresented:)` wrapping `NewConversationSheet`. Nav strategy: on create, channel appears at top of list; user taps to enter (avoids programmatic NavigationStack refactor).
  - Commits: Web `08bce19 feat(supabase): chat conversation RPC for cross-client creation`, App `7143142 feat(chat): create-conversation + find-or-create DM via RPC (Sprint 3.2)`. `xcodebuild build -destination "generic/platform=iOS Simulator"` → `** BUILD SUCCEEDED **`. Per cadence directive ("先做 1:1 移植和 app 前端, 最后统一测试"), iOS-device smoke + Winston 3.2 audit + staging-shadow validation batched to the final unified testing phase.
  - Debt-04 partial close: create-conversation + find-or-create-DM ✅ CLOSED. `@mentions` (naive regex + `chat_mentions` write) and message search (`ilike` on `content`) still deferred.
- [x] 3.2a Chat RLS Tightening — Task A (migration) + Task B (Web read-path regression audit) + Task C1 (local RLS semantic verification via native Postgres): all green under repeated "你自己决定" autonomous execution.
  - Task A: `BrainStorm+-Web/supabase/migrations/20260421000000_chat_rls_tightening.sql` — DROPs `"Anyone can view channels"` / `"Users can view messages"` (`USING (true)`), CREATEs `chat_channels_select_membership` (three-way union `announcement ∪ owned ∪ member-of`, mirrors Web `chat.ts:252-282`) + `chat_messages_select_membership` (containing-channel visibility via `EXISTS`). Timestamp filename convention (not serial 029) matches latest migration `20260418110000`.
  - Task B: three parallel Explore subagents audited `BrainStorm+-Web/src/` chat read paths. `docs/parity/54-3.2a-task-b-web-regression-audit.md` consolidates: **23 admin-client reads** in `src/lib/actions/chat.ts` ✅ unaffected (service_role bypass); **1 filtered user-role Realtime read** (`dashboard/chat/page.tsx:200` channel_id-filtered, RLS-compatible); **1 global user-role Realtime read** (`notification-provider.tsx:81-88` `@mention` listener) narrows to member-channels — **strict UX improvement** (removes dead-link notifications for non-member channels). Zero pre-migration code changes required. Surfaced debt-10 (`chat_channel_members` SELECT = `auth.uid() IS NOT NULL`, precondition for new EXISTS subqueries; tightening requires SECURITY DEFINER or redesign).
  - Task C1: Docker daemon unavailable for `supabase start`, so verified via native Postgres 15 + `auth.uid()` stub (session GUC `request.jwt.claim.sub`) in `/tmp/chat_rls_test.sh`. Four-user scenario matrix (DM members A + B, C-team owner C, stranger D) all green; stranger sees only announcement (0 non-announcement leaked). Intermediate `SET LOCAL`-outside-transaction harness bug caught — silent `auth.uid()=NULL` collapse incidentally confirmed policies deny anonymous access. Post-fix re-run all green. Evidence: `docs/parity/55-3.2a-task-c1-local-verification.md`. Full-stack Supabase verification deferred to Docker-available run or folds into C3 staging shadow.
- [x] 3.3 Chat Attachments (image + file subset of 3.1-debt-01) completed autonomously under "你自己决定" + "先做 1:1 移植和 app 前端, 最后统一测试" cadence directives. Web source-of-truth scan confirmed the production upload path is `/api/chat/upload` (route.ts:9 uses bucket `'chat-files'` via `createAdminClient()`); the `page.tsx:288` reference to `'chat_attachments'` is dead code (bucket doesn't exist, unreachable branch). Model: `Core/Models/ChatModel.swift` adds public `ChatAttachment: Codable, Hashable { name, url, type (MIME), size: Int? }` + `isImage` computed + extends `ChatMessage` with `attachments: [ChatAttachment]` via null-tolerant custom decoder (`try? decodeIfPresent ... ?? []` — matches Web `normalizeAttachments` chat.ts:169-185). ViewModel: `Features/Chat/ChatRoomViewModel.sendMessage(_:attachments:)` now accepts `[ChatAttachment] = []`, drops the empty-text guard (attachments-only sends allowed), derives `type` via `attachments.isEmpty ? "text" : (allSatisfy({ $0.isImage }) ? "image" : "file")` (1:1 with chat.ts:595-597), `last_message` preview text-or-`"[图片]"`/`"[文件]"`. New `uploadAttachment(data:fileName:mimeType:)` writes to bucket `chat-files` at path `{user_id}/{channel_id}/{uuid}.{ext}` via user-JWT — first segment satisfies existing storage RLS (migration 028:42-58 `auth.uid()::text = (storage.foldername(name))[1]`) without needing admin client, channel segment preserves archival semantics, UUID+ext prevents collisions. Returns `ChatAttachment` with `getPublicURL(path:).absoluteString` (public bucket → no signed-URL TTL). View: `Features/Chat/ChatRoomView.swift` + button becomes `Menu` wrapping `PhotosPicker(selection: $photoItems, maxSelectionCount: 9, matching: .images)` + `Button → .fileImporter(allowedContentTypes: [.item], allowsMultipleSelection: true)`. Photos: `.onChange(of: photoItems) → PhotosPickerItem.loadTransferable(type: Data.self)` → `PendingUpload(data, fileName: "IMG_<uuid>.jpg", mimeType: "image/jpeg", previewImage: UIImage(data:))`. Files: `startAccessingSecurityScopedResource()` + `Data(contentsOf:)` + `UTType(filenameExtension:)?.preferredMIMEType` MIME derivation. Horizontal pending strip above input bar: 64×64 thumbs (UIImage for images, `doc.fill`+name for files) + `xmark.circle.fill` remove chips. `sendTapped` awaits each `uploadAttachment` sequentially, collects into `[ChatAttachment]`, calls `sendMessage(text, attachments:)`; upload failure → `errorMessage` banner without consuming pendingUploads. `messageBubble` grows: `msg.isWithdrawn` → italic gray "此消息已撤回" bubble with no attachments rendered (Web parity); else text bubble (skipped when content empty — no empty bubble over attachments-only) + `ForEach(attachments)` → `AsyncImage` 200×200 rounded for images, `doc.fill`+filename row for files, both wrapped in `Link(destination: URL)` for tap-to-open (Safari / system quick-look). Build: `xcodebuild build -destination "generic/platform=iOS Simulator" -configuration Debug` → `** BUILD SUCCEEDED **` (SourceKit inline red marks on Supabase/UIKit/ChatAttachment types were VSCode-indexer false positives; real compiler accepted everything). Commit: App `93ad38f feat(chat): image/file attachment upload + render (Sprint 3.3)` (+414/-24 across 3 files). No Web or DB changes needed — `chat-files` bucket + RLS + `chat_messages.attachments` JSONB all pre-existed from migration 028. Debt-01 partial close: image + file subset ✅ CLOSED; explicitly deferred to 3.3-followup are image compression, signed-URL reads, upload progress bar, inline quick-look preview, multi-image gallery modal, server-side size/extension enforcement. Per cadence directive, Winston 3.3 audit + iOS staging smoke batched to final unified testing phase.
- Next step (3.2a): C2 iOS staging smoke (requires staging project with migration applied) + C3 staging shadow ≥24h (watch notification-provider global `@mention` listener + chat-page realtime for continued in-member delivery, chat table SELECT error rate). After C2+C3 green, decide Task D scope (D1 annotate iOS client-side `getAccessibleChannelMap` mirror as "DB-enforced, kept as defense in depth" vs D2 aggressively collapse to no-op pass-through). Winston 3.2a re-audit triggers after A+B+C+D all land.
- [x] 3.4 Chat Debt Cleanup (withdraw + reply + search + lazy nav) completed autonomously under repeated "你自己决定" + "先做 1:1 移植和 app 前端, 最后统一测试" + "其实你可以继续自动进行一长段的开发" cadence directives. Closed 4 open-debt items from 3.1 in a single sprint.
  - **Web**: migration `BrainStorm+-Web/supabase/migrations/20260421150000_chat_withdraw_rpc.sql` adds SECURITY DEFINER RPC `chat_withdraw_message(p_message_id UUID) RETURNS VOID`. Replicates Web `withdrawMessage` (chat.ts:702-741) server-side checks (auth + sender match + not-already-withdrawn + 2-min window). Post-UPDATE mirrors Web chat.ts:722-728 exactly: `content='此消息已撤回'`, `attachments='[]'::jsonb`, `is_withdrawn=TRUE`, `withdrawn_at=now()`, `reactions` preserved. SQLSTATEs: `42501` unauth, `P0002` not-found, `22023` not-sender / already-withdrawn / outside-window. `REVOKE FROM PUBLIC; GRANT EXECUTE TO authenticated;`. Rationale: `chat_messages` has NO UPDATE RLS policy (migration 028 intentionally server-gates mutations); Web bypasses via admin client; iOS user-JWT needs SECURITY DEFINER. Side benefit: 2-min window moves from Web-client-only (`page.tsx:386-390`, tamperable) to DB-enforced. Web commit `4f99516`.
  - **iOS** (App commit `334fc38`, +461/-59 across 4 files):
    - **Withdraw** (debt-02): `ChatRoomViewModel.withdrawMessage(_ messageId:)` calls RPC via `client.rpc("chat_withdraw_message", params: Params(p_message_id:))`; no optimistic mutation (realtime UPDATE is authoritative). `subscribeRealtime()` adds a second `UpdateAction` stream on the same channel (both feeds share the WS subscription), `withTaskGroup` fans out two `for await` loops; `handleRealtimeUpdate` patches `messages[]` + `replyLookup` in place by id. `ChatRoomView` bubble `contextMenu` shows "回复" + "撤回" — `撤回` only shown when `isCurrentUser && !isWithdrawn && Date().timeIntervalSince(createdAt ?? .distantPast) < 120` so UI guards match RPC guards.
    - **Reply threading** (debt-03): no DB changes — `reply_to` FK already present. `fetchMessages()` chains `hydrateReplyLookup(for:)` which collects unique missing parent ids and issues a single `.in("id", values:)` select into `@Published replyLookup: [UUID: ChatMessage]`. `sendMessage(_:attachments:replyTo:)` accepts optional `replyTo: UUID?`, encodes `reply_to` as `Optional<String>` in `InsertPayload` (PostgREST emits `null` for nil, avoiding UUID cast failure). `handleRealtimeInsert` backfills replyLookup for new replies whose parent isn't in cache. `ChatRoomView` adds `replyingTo: ChatMessage?` state, `replyPreviewStrip(parent:)` above input bar (dismiss via x), and in-bubble `replyBlock(parentId:)` rendering grey-bar + single-line preview with degradation chain: text / `[图片]` / `[文件]` / `消息已撤回` / `原消息不可用`. `sendTapped` forwards `replyingTo?.id` and clears optimistically.
    - **Search** (debt-04 search subset): `ChatListViewModel.searchMessages(query:)` does `.in(accessibleChannelIds)` + `.ilike("content", "%q%")` + `.eq("is_withdrawn", false)` + `order DESC` + `limit 20`; joins channels in-memory (`Dictionary(uniqueKeysWithValues:)`) into new public `ChatSearchResult: Identifiable, Hashable { message, channel }`. Min 2-char guard. `self.channels` acts as the accessible-channel whitelist (empty until `fetchChannels()` completes). `ChatListView` gains `.searchable(text: $viewModel.searchQuery, prompt: "搜索消息…")` with 250ms debounce via `Task.sleep + isCancelled + query-equality recheck` (chosen over `.task(id:)` which would cancel mid-typed edits). Non-empty query replaces channel list with search-results list; `ContentUnavailableView.search(text:)` on empty results; result taps reuse value-based nav to jump to the message's channel.
    - **Nav laziness** (debt-08): `ChatListView` migrated from eager `NavigationLink(destination: ChatRoomView(viewModel: ChatRoomViewModel(client:channel:)))` per row to `NavigationLink(value: channel)` + `.navigationDestination(for: ChatChannel.self) { ch in ChatRoomView(viewModel: ChatRoomViewModel(client: supabase, channel: ch)) }` at the NavigationStack root. `ChatChannel` was already `Hashable`. ChatRoomViewModel now constructed only when the destination actually renders.
  - **Build**: `xcodebuild -scheme Brainstorm+ -destination "generic/platform=iOS Simulator" -configuration Debug build` → `** BUILD SUCCEEDED **`. Pre-existing Swift 6 concurrency warnings unchanged; zero new diagnostics from 3.4 changes. Compile error during iteration: `canWithdraw` initially used `msg.createdAt` as non-optional Date; ChatMessage's `createdAt` is `Date?` so added nil-guard returning false (conservative: missing timestamp → disable withdraw affordance).
  - **Deferred**: `@mentions` (debt-04 remaining subset, rolls to Sprint 3.5 — Web parses `content.includes('@<name>')` at send time and writes `chat_mentions` table; iOS still plain text). Search highlighting / pagination / withdraw-undo / cross-window reply-parent lazy load all explicitly out-of-scope. Winston 3.4 audit + staging smoke batched to final unified testing phase per standing directive.
  - **Debt status after 3.4**: original 9 debts from 3.1 — 01 (closed 3.3), 02 (closed 3.4), 03 (closed 3.4), 04 (search subset closed 3.4; `@mentions` still open), 05 (3.2a A+B+C1 done; C2+C3+D pending staging), 06+07 (closed 3.1 closeout), 08 (closed 3.4), 09 (mock-Supabase testability still open). **Active open debts: debt-04 `@mentions` subset + debt-05 3.2a C2/C3/D remainder + debt-09 testability.**
- [x] 3.5 Chat Content Highlighting (@mention + search-result substring) completed autonomously under the same cadence directives. Closed the final open slice of 3.1-debt-04.
  - **Source-of-truth re-check**: before writing code, re-grepped Web for `chat_mentions` and mention-server-write behavior. Zero hits in `BrainStorm+-Web/supabase/migrations/**` and `BrainStorm+-Web/src/lib/actions/chat.ts` — Web's @mention support is **purely** `text.split(/(@\S+)/g).map(part => part.startsWith('@') ? <span .../> : part)` in `src/app/dashboard/chat/page.tsx:31-39`. The 3.4-closeout carry-forward note overstated this ("Web parses `content.includes('@<name>')` and writes `chat_mentions`") — fact-check caught it before it turned into speculative schema + notification work.
  - **Implementation** (App commit `624d8d7`, +108/-5 across 3 files):
    - **New file `Brainstorm+/Features/Chat/ChatContentHighlighter.swift`** (77 lines): public `enum ChatContentHighlighter` (enum, not struct — zero-state helper) with single public API `attributed(_ raw: String, searchTerm: String? = nil, mentionColor: Color = .blue, matchBackground: Color = Color.yellow.opacity(0.45)) -> AttributedString`. Two passes on a local `AttributedString(raw)`: (1) `NSRegularExpression(pattern: #"@\S+"#)` mutates each match's font + foregroundColor, (2) optional case-insensitive literal match using `(?i)<NSRegularExpression.escapedPattern(for: term)>` mutates backgroundColor. Only applies search pass when trimmed term ≥ 2 chars (mirrors `searchMessages` guard). Index conversion goes `NSRange` → `Range(_:in: raw)` → char-offset via `raw.distance(from:to:)` → `attr.index(attr.startIndex, offsetByCharacters:)` because NSRange is UTF-16-indexed and `offsetByCharacters` is char-indexed — documented inline so the next editor doesn't simplify it into a broken shortcut.
    - **`ChatRoomView.swift`**: message bubble `Text(msg.content)` → `Text(ChatContentHighlighter.attributed(msg.content, mentionColor: isCurrentUser ? .yellow : .blue))`. Parametric mention color because self-bubbles are blue-background + white-text (yellow mention reads, blue mention vanishes) and peer-bubbles are light-grey + primary-text (blue mention reads). Reply-block / withdrawn-placeholder / attachment rows untouched.
    - **`ChatListView.swift`**: search-result row's content `Text` routes through the highlighter with `searchTerm: viewModel.searchQuery` for substring highlight. Empty-content rows still render `[附件]` as a plain `Text` (explicit else-branch, not highlighted). Channel-header row + timestamp unchanged.
  - **Web / DB / RLS**: no changes. Pure iOS visual layer.
  - **Build**: `xcodebuild -scheme "Brainstorm+" -destination "platform=iOS Simulator,name=iPhone 17 Pro" build` → `** BUILD SUCCEEDED **`. Zero new warnings; pre-existing Swift 6 concurrency warnings unchanged. (SourceKit flashed the usual "No such module 'Supabase'" / "Cannot find 'UIColor'" on edit, ignored per established VSCode-indexer-is-not-authoritative pattern.)
  - **Deferred (explicit)**: (1) Mention completion picker — Web doesn't have one either. (2) User resolution / tap-to-profile on mentions — both platforms skip. (3) Matched-mention push notifications — neither platform does this today; requires schema + trigger nobody's signed off on; reopen when product asks. (4) Search-term re-highlight inside the room view — when user taps a search result, the bubble does not re-highlight the matched substring because ChatRoomView has no awareness of the search term from ChatListView; acceptable since the query stays visible in the `.searchable` field if user backs out.
  - **Debt status after 3.5**: 3.1-debt-04 fully ✅ CLOSED (all four subsets: mentions + search + create-conversation + find-or-create-DM). **Active open debts: debt-05 3.2a C2/C3/D remainder (cross-stack security sprint, needs staging) + debt-09 mock-Supabase testability harness (no longer tied to code work, needs a dedicated sprint).**
- [x] 4.1 Approvals Foundation READ (我提交的) completed autonomously under repeated cadence directives. First iOS surface for the Approvals module — read-only user-submissions list, mirrors Web `src/app/dashboard/approval/_tabs/my-submissions.tsx`. Full 4-sprint scope split pre-committed to `docs/parity/56-approvals-foundation-scope.md` (sprint 4.2 = detail + revoke-comp-time, 4.3 = approver queue + approve/reject, 4.4 = submission forms).
  - **Source-of-truth scan**: 2500 LOC across 11 Web files — this sprint only ports `my-submissions.tsx` (185 lines) + `listMySubmissions` (approval-requests.ts:555-621) + narrow enum subset from `src/lib/approvals/types.ts`. RLS posture validated in migrations 020/027: `approval_requests` SELECT allows `auth.uid() = requester_id` directly via user-JWT; **no admin-client bypass needed on iOS**, unlike Web's `createAdminClient()`.
  - **Architecture choices (autonomous)**:
    - ViewModel split: `MySubmissionsViewModel` owns only this screen; 4.3's approver queue gets its own `ApprovalQueueViewModel` (no god-object).
    - Single-file model `Core/Models/ApprovalModel.swift` — `ApprovalMySubmissionRow` + `ApprovalLeaveDetail` + four enums. 4.2 widens the same file with reimbursement + procurement detail structs.
    - Forward-compatible enum decoding: `unknown` escape-hatch case + custom `init(from:)` maps unknown raw → `.unknown`. Mirrors Web's `TYPE_LABEL[r.request_type] ?? r.request_type` fallback.
    - Resilient nested-select decoder for `approval_request_leave` — accepts array | single object | missing (PostgREST response shape varies with FK uniqueness inference). Matches Web's defensive handling (approval-requests.ts:596-614).
    - Date columns kept as `String "YYYY-MM-DD"` per `Attendance.swift:6` convention — Postgres `date` type is returned unquoted by Supabase, default ISO8601 decoder expects time component.
    - `ApprovalStatus.Tone` nested enum drives chip tint without forcing SwiftUI into `Core/Models` — view-layer maps tone → Color.
  - **Model** (`Brainstorm+/Core/Models/ApprovalModel.swift`, 230 lines): `ApprovalMySubmissionRow` (9 columns + `leaveDetails: [ApprovalLeaveDetail]` + `leave: ApprovalLeaveDetail?` convenience) + `ApprovalLeaveDetail` (4-column narrow projection) + `ApprovalRequestType` (6 variants + unknown + displayLabel matching Web `TYPE_LABEL`) + `ApprovalStatus` (9 variants + unknown + `Tone` nested enum + displayLabel matching Web `STATUS_META`) + `RequestPriority` (4 variants + unknown, decoded but not rendered in 4.1) + `LeaveType` (8 variants + unknown + displayLabel).
  - **ViewModel** (`Brainstorm+/Features/Approvals/MySubmissionsViewModel.swift`, 77 lines): `@MainActor public final class` with private(set) `rows`/`isLoading` + mutable `errorMessage` (for `.zyErrorBanner` dismissal). `listMySubmissions(limit: Int = 100) async` issues the Web-parity nested select + `.eq("requester_id", ...)` + `.order("created_at", descending)` + `.limit(limit)`.
  - **View** (`Brainstorm+/Features/Approvals/ApprovalsListView.swift`, 175 lines): `@StateObject`-owned ViewModel, loading / empty / populated tri-state. Row = (type label + status chip + created-at) / leave preview (conditional on `.leave`) / business_reason (2-line clamp) / reviewer_note with leading grey bar. `.zyErrorBanner` + `.refreshable` + `.task` three-modifier stack matches `ChatListView`. Leave preview: `"{leaveType} · {startDate} ~ {endDate} · {days} 天"` (skips leaveType segment when `.unknown`). Status chip tint via view-layer Color mapping from `ApprovalStatus.Tone`.
  - **Entry wiring**: `Features/Dashboard/ActionItemHelper.swift` `.approval` case now returns `ApprovalsListView(viewModel: MySubmissionsViewModel(client: supabase))` (was `ParityBacklogDestination`). `Features/Dashboard/DashboardView.swift` quick-actions grid gains a pink-tinted Approvals tile.
  - **Build**: `xcodebuild -project "Brainstorm+.xcodeproj" -scheme "Brainstorm+" -destination "platform=iOS Simulator,name=iPhone 17 Pro" build` → `** BUILD SUCCEEDED **`. SourceKit false positives about `Color.Brand` / `ActionItemHelper` / `HapticManager` scope ignored (VSCode indexer runs outside Xcode SPM graph; xcodebuild is authoritative).
  - **Commits**: scope doc `e457dd5`, model `5c801eb`, App (ViewModel + View + wiring) `196f37b`. No Web / migration / RLS changes — 020/027 RLS + existing `approval_requests` + `approval_request_leave` tables satisfy the read path.
  - **Deferred (explicit, carry-forward to 4.2+)**: detail dialog / revoke-comp-time self-service / approver queues (6 sub-tabs) / approve+reject write paths / submission forms (leave + reimbursement + procurement) / push notifications on status change / Winston 4.1 + staging smoke. All pre-committed to `docs/parity/56-approvals-foundation-scope.md` with sprint numbering.

## Phase 1.x: Subsequent Features
- [ ] Migrate remaining Dashboard navigation components to strict module.
- [ ] Projects parity beyond list-scope + detail-gate + owner-join + read-only detail-enrichment + nested-profile-names + owner-avatar + edit + member-management + delete + task-count-on-list + AI-summary-foundation + risk-analysis-foundation + linked-risk-actions-foundation + resolution-feedback-foundation + risk-action-sync-write-path-foundation foundations: resolution write-back (close / reopen / effectiveness / governance note) / real LLM-generated narrative / generate-risk-from-iOS. 2.0 closed native delete flow. 2.1 closed task-count-on-list via a batched non-N+1 aggregate (source-of-truth discrepancy recorded: Web's `task_count` typed field is vestigial). 2.2 closed AI summary foundation on detail via parallel `async let` PostgREST reads + deterministic facts-only local synthesis + isolated `summaryErrorMessage`; recorded a second-order source-of-truth discrepancy because Web's `generateProjectSummary` is a server action with no HTTP exposure, so iOS cannot call `askAI()` directly and must not embed decrypted provider credentials on-device — LLM-generated narrative deferred until a server-side `/api/ai/project-summary` endpoint is published. 2.3 closed risk-analysis-foundation on detail via a cache-first read of `project_risk_summaries` + read-only risk-level badge + provenance caption + isolated `riskAnalysisErrorMessage`; recorded a second-order source-of-truth discrepancy WITH persistence parity — iOS reads actual Web-generated analysis rather than synthesizing locally, but cannot generate fresh analyses until Web exposes `/api/ai/project-risk` (or a Supabase Edge Function). 2.4 closed linked-risk-actions-foundation on detail via two-step anchor-then-filter PostgREST reads (`project_risk_summaries.select("id")` → `risk_actions.eq("ai_source_id", anchor.id).order(desc).limit(20)`) + 5-phase state machine (idle / loading / noRiskAnalysisSource / empty / loaded) + isolated `linkedRiskActionsErrorMessage` + `priorPhase` revert on failure + 3-row render cap matching Web exactly. **No source-of-truth discrepancy** — `getLinkedRiskActions` is a pure read flow. 2.5 delivered most resolution-feedback-foundation mechanics on detail via the same two-step anchor-then-filter PostgREST read pattern (`project_risk_summaries.select("id")` → `risk_actions.select(...8 columns...).eq("ai_source_id", anchor.id).order("resolved_at", ascending: false, nullsFirst: false).limit(50)`) + client-side aggregation (counts + `dominantCategory` with first-encountered tie-break + top-3 `recentResolutions` filtered to `{resolved, dismissed}`) + predictive `isProneToReopen` (mirrors Web's "易重开" trigger) + 5-phase state machine + isolated `resolutionFeedbackErrorMessage` + `priorPhase` revert on failure. However Winston 2.5 audit found one blocking source-of-truth parity issue: Web governance intervention status is `hasEffective`-first, while iOS 2.5 made `.needsIntervention` win when both signals fire. 2.5.1 closed the fix via two minimal edits: `ProjectDetailModels.swift` `governanceSignal` now evaluates `effective` first and only checks `needs` when `effective` is false (matches Web's `hasEffective` short-circuit), and `ProjectDetailView.swift` switched the prone-to-reopen subline tint to `Color.Brand.warning` so the predictive "易重开" indicator stays on an independent semantic rail regardless of which governance banner tone is active. `isProneToReopen` trigger unchanged. 2.6 closed the first iOS write path via **Path A** (direct Supabase INSERT): `ProjectDetailViewModel.syncRiskActionFromDetail(rawRole:)` does RBAC gate (raw-string check against `{super_admin, admin, hr_admin, manager}` — `RBACManager.migrateLegacyRole` drops `hr_admin` and over-maps `chairperson`/`team_lead`, so `PrimaryRole` could not be used directly) → anchor re-fetch → `profiles.select("org_id")` → `risk_actions.insert(...).select("id").single()` with 11 hard-coded / user-supplied columns (Chinese `风险项` + `从项目风险分析生成` persistence parity with Web) → best-effort `risk_action_events` audit (swallowed on failure) → fire-and-forget `refreshLinkedRiskActions()` + `refreshResolutionFeedback()`. UI: `@ViewBuilder riskActionSyncAffordance` inside `riskAnalysisSection` with `.confirmationDialog` showing title/severity/detail preview, 3-phase `RiskActionSyncPhase { idle, syncing, succeeded }`, isolated `riskActionSyncErrorMessage`, 3-second auto-clear via `scheduleRiskActionSyncSuccessClear()` (mirrors Web's `setTimeout(..., 3000)`). Resolution write-back (close / reopen / effectiveness / governance note) + generate-risk-from-iOS + LLM narrative remain explicitly deferred by scope.
- [ ] Approvals 4.2 (detail + revoke-comp-time) / 4.3 (approver queue + approve+reject) / 4.4 (submission forms). Admin.
